// Generated by tools/generate_conformance.py. Do not edit by hand.
#include "knotwork/animation.hpp"
#include "knotwork/bounds.hpp"
#include "knotwork/builder.hpp"
#include "knotwork/diagnostics.hpp"
#include "knotwork/flatten.hpp"
#include "knotwork/graph.hpp"
#include "knotwork/json.hpp"
#include "knotwork/query.hpp"
#include "knotwork/text.hpp"

#include <cmath>
#include <iostream>
#include <string>
#include <vector>

namespace knotwork_generated {

struct Failure {
  std::string name;
  std::string message;
};

std::vector<Failure>& failures() {
  static std::vector<Failure> data;
  return data;
}

void check(bool condition, const std::string& name, const std::string& message) {
  if (!condition) {
    failures().push_back({name, message});
  }
}

bool close(float a, float b) {
  return std::fabs(a - b) < 0.001f;
}

}  // namespace knotwork_generated


void generated_case_0000() {
  using namespace knotwork;
  const std::string name = "generated_case_0000";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_0", {0.000f, 0.000f, 0.000f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_0", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_0", root, mat, trs({1.000f, 0.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_0", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_0", root, LightKind::Point, 1.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_0", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 1.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 0.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_0") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({1.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_0";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 2.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0001() {
  using namespace knotwork;
  const std::string name = "generated_case_0001";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_1", {0.500f, 0.250f, 0.167f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_1", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_1", root, mat, trs({2.000f, 0.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_1", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_1", root, LightKind::Point, 2.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_1", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 3.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 2.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_1") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({3.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_1";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 4.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0002() {
  using namespace knotwork;
  const std::string name = "generated_case_0002";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_2", {1.000f, 0.500f, 0.333f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_2", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_2", root, mat, trs({3.000f, 0.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_2", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_2", root, LightKind::Point, 3.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_2", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 5.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_2") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({5.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_2";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 6.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0003() {
  using namespace knotwork;
  const std::string name = "generated_case_0003";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_3", {0.000f, 0.750f, 0.500f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_3", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_3", root, mat, trs({4.000f, 0.750f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_3", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_3", root, LightKind::Point, 4.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_3", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 6.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_3") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_3";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0004() {
  using namespace knotwork;
  const std::string name = "generated_case_0004";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_4", {0.500f, 1.000f, 0.667f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_4", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_4", root, mat, trs({5.000f, 1.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_4", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_4", root, LightKind::Point, 5.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_4", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_4") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_4";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0005() {
  using namespace knotwork;
  const std::string name = "generated_case_0005";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_5", {1.000f, 0.000f, 0.833f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_5", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_5", root, mat, trs({6.000f, 1.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_5", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_5", root, LightKind::Point, 6.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_5", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 10.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_5") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_5";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0006() {
  using namespace knotwork;
  const std::string name = "generated_case_0006";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_6", {0.000f, 0.250f, 1.000f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_6", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_6", root, mat, trs({7.000f, 1.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_6", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_6", root, LightKind::Point, 7.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_6", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 12.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_6") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_6";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0007() {
  using namespace knotwork;
  const std::string name = "generated_case_0007";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_7", {0.500f, 0.500f, 0.000f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_7", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_7", root, mat, trs({8.000f, 0.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_7", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_7", root, LightKind::Point, 8.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_7", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 14.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_7") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_7";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0008() {
  using namespace knotwork;
  const std::string name = "generated_case_0008";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_8", {1.000f, 0.750f, 0.167f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_8", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_8", root, mat, trs({9.000f, 0.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_8", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_8", root, LightKind::Point, 9.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_8", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 16.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_8") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_8";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0009() {
  using namespace knotwork;
  const std::string name = "generated_case_0009";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_9", {0.000f, 1.000f, 0.333f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_9", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_9", root, mat, trs({10.000f, 0.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_9", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_9", root, LightKind::Point, 10.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_9", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 18.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_9") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_9";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0010() {
  using namespace knotwork;
  const std::string name = "generated_case_0010";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_10", {0.500f, 0.000f, 0.500f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_10", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_10", root, mat, trs({11.000f, 0.750f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_10", root, 55.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_10", root, LightKind::Point, 11.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_10", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 20.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_10") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_10";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0011() {
  using namespace knotwork;
  const std::string name = "generated_case_0011";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_11", {1.000f, 0.250f, 0.667f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_11", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_11", root, mat, trs({1.000f, 1.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_11", root, 56.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_11", root, LightKind::Point, 12.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_11", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 11.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_11") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_11";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0012() {
  using namespace knotwork;
  const std::string name = "generated_case_0012";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_12", {0.000f, 0.500f, 0.833f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_12", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_12", root, mat, trs({2.000f, 1.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_12", root, 57.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_12", root, LightKind::Point, 13.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_12", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 13.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_12") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_12";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0013() {
  using namespace knotwork;
  const std::string name = "generated_case_0013";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_13", {0.500f, 0.750f, 1.000f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_13", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_13", root, mat, trs({3.000f, 1.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_13", root, 58.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_13", root, LightKind::Point, 14.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_13", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 28.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_13") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_13";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0014() {
  using namespace knotwork;
  const std::string name = "generated_case_0014";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_14", {1.000f, 1.000f, 0.000f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_14", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_14", root, mat, trs({4.000f, 0.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_14", root, 59.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_14", root, LightKind::Point, 15.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_14", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 30.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_14") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_14";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0015() {
  using namespace knotwork;
  const std::string name = "generated_case_0015";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_15", {0.000f, 0.000f, 0.167f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_15", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_15", root, mat, trs({5.000f, 0.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_15", root, 60.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_15", root, LightKind::Point, 16.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_15", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 32.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_15") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_15";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0016() {
  using namespace knotwork;
  const std::string name = "generated_case_0016";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_16", {0.500f, 0.250f, 0.333f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_16", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_16", root, mat, trs({6.000f, 0.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_16", root, 61.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_16", root, LightKind::Point, 17.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_16", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 22.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 34.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_16") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({22.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_16";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 23.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0017() {
  using namespace knotwork;
  const std::string name = "generated_case_0017";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_17", {1.000f, 0.500f, 0.500f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_17", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_17", root, mat, trs({7.000f, 0.750f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_17", root, 62.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_17", root, LightKind::Point, 18.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_17", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 2.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_17") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_17";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0018() {
  using namespace knotwork;
  const std::string name = "generated_case_0018";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_18", {0.000f, 0.750f, 0.667f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_18", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_18", root, mat, trs({8.000f, 1.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_18", root, 63.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_18", root, LightKind::Point, 19.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_18", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_18") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_18";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0019() {
  using namespace knotwork;
  const std::string name = "generated_case_0019";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_19", {0.500f, 1.000f, 0.833f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_19", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_19", root, mat, trs({9.000f, 1.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_19", root, 64.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_19", root, LightKind::Point, 20.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_19", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 6.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_19") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_19";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0020() {
  using namespace knotwork;
  const std::string name = "generated_case_0020";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_20", {1.000f, 0.000f, 1.000f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_20", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_20", root, mat, trs({10.000f, 1.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_20", root, 65.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_20", root, LightKind::Point, 1.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_20", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_20") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_20";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0021() {
  using namespace knotwork;
  const std::string name = "generated_case_0021";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_21", {0.000f, 0.250f, 0.000f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_21", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_21", root, mat, trs({11.000f, 0.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_21", root, 66.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_21", root, LightKind::Point, 2.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_21", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 10.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_21") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_21";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0022() {
  using namespace knotwork;
  const std::string name = "generated_case_0022";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_22", {0.500f, 0.500f, 0.167f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_22", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_22", root, mat, trs({1.000f, 0.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_22", root, 67.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_22", root, LightKind::Point, 3.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_22", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 6.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_22") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({6.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_22";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 7.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0023() {
  using namespace knotwork;
  const std::string name = "generated_case_0023";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_23", {1.000f, 0.750f, 0.333f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_23", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_23", root, mat, trs({2.000f, 0.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_23", root, 68.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_23", root, LightKind::Point, 4.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_23", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_23") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_23";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0024() {
  using namespace knotwork;
  const std::string name = "generated_case_0024";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_24", {0.000f, 1.000f, 0.500f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_24", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_24", root, mat, trs({3.000f, 0.750f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_24", root, 69.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_24", root, LightKind::Point, 5.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_24", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_24") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_24";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0025() {
  using namespace knotwork;
  const std::string name = "generated_case_0025";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_25", {0.500f, 0.000f, 0.667f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_25", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_25", root, mat, trs({4.000f, 1.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_25", root, 70.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_25", root, LightKind::Point, 6.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_25", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_25") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_25";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0026() {
  using namespace knotwork;
  const std::string name = "generated_case_0026";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_26", {1.000f, 0.250f, 0.833f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_26", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_26", root, mat, trs({5.000f, 1.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_26", root, 71.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_26", root, LightKind::Point, 7.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_26", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 22.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_26") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_26";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0027() {
  using namespace knotwork;
  const std::string name = "generated_case_0027";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_27", {0.000f, 0.500f, 1.000f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_27", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_27", root, mat, trs({6.000f, 1.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_27", root, 72.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_27", root, LightKind::Point, 8.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_27", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 24.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_27") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_27";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0028() {
  using namespace knotwork;
  const std::string name = "generated_case_0028";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_28", {0.500f, 0.750f, 0.000f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_28", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_28", root, mat, trs({7.000f, 0.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_28", root, 73.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_28", root, LightKind::Point, 9.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_28", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 26.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_28") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_28";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0029() {
  using namespace knotwork;
  const std::string name = "generated_case_0029";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_29", {1.000f, 1.000f, 0.167f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_29", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_29", root, mat, trs({8.000f, 0.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_29", root, 74.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_29", root, LightKind::Point, 10.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_29", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 28.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_29") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_29";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0030() {
  using namespace knotwork;
  const std::string name = "generated_case_0030";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_30", {0.000f, 0.000f, 0.333f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_30", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_30", root, mat, trs({9.000f, 0.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_30", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_30", root, LightKind::Point, 11.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_30", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 22.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 30.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_30") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({22.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_30";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 23.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0031() {
  using namespace knotwork;
  const std::string name = "generated_case_0031";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_31", {0.500f, 0.250f, 0.500f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_31", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_31", root, mat, trs({10.000f, 0.750f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_31", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_31", root, LightKind::Point, 12.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_31", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 24.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 32.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_31") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({24.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_31";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 25.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0032() {
  using namespace knotwork;
  const std::string name = "generated_case_0032";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_32", {1.000f, 0.500f, 0.667f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_32", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_32", root, mat, trs({11.000f, 1.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_32", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_32", root, LightKind::Point, 13.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_32", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 26.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 34.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_32") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({26.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_32";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 27.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0033() {
  using namespace knotwork;
  const std::string name = "generated_case_0033";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_33", {0.000f, 0.750f, 0.833f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_33", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_33", root, mat, trs({1.000f, 1.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_33", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_33", root, LightKind::Point, 14.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_33", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 25.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_33") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_33";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0034() {
  using namespace knotwork;
  const std::string name = "generated_case_0034";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_34", {0.500f, 1.000f, 1.000f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_34", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_34", root, mat, trs({2.000f, 1.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_34", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_34", root, LightKind::Point, 15.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_34", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 2.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_34") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({2.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_34";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 3.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0035() {
  using namespace knotwork;
  const std::string name = "generated_case_0035";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_35", {1.000f, 0.000f, 0.000f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_35", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_35", root, mat, trs({3.000f, 0.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_35", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_35", root, LightKind::Point, 16.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_35", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 4.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_35") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({4.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_35";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 5.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0036() {
  using namespace knotwork;
  const std::string name = "generated_case_0036";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_36", {0.000f, 0.250f, 0.167f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_36", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_36", root, mat, trs({4.000f, 0.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_36", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_36", root, LightKind::Point, 17.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_36", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 6.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_36") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({6.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_36";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 7.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0037() {
  using namespace knotwork;
  const std::string name = "generated_case_0037";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_37", {0.500f, 0.500f, 0.333f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_37", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_37", root, mat, trs({5.000f, 0.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_37", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_37", root, LightKind::Point, 18.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_37", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_37") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_37";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0038() {
  using namespace knotwork;
  const std::string name = "generated_case_0038";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_38", {1.000f, 0.750f, 0.500f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_38", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_38", root, mat, trs({6.000f, 0.750f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_38", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_38", root, LightKind::Point, 19.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_38", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_38") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_38";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0039() {
  using namespace knotwork;
  const std::string name = "generated_case_0039";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_39", {0.000f, 1.000f, 0.667f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_39", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_39", root, mat, trs({7.000f, 1.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_39", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_39", root, LightKind::Point, 20.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_39", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 16.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_39") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_39";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0040() {
  using namespace knotwork;
  const std::string name = "generated_case_0040";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_40", {0.500f, 0.000f, 0.833f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_40", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_40", root, mat, trs({8.000f, 1.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_40", root, 55.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_40", root, LightKind::Point, 1.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_40", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 18.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_40") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_40";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0041() {
  using namespace knotwork;
  const std::string name = "generated_case_0041";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_41", {1.000f, 0.250f, 1.000f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_41", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_41", root, mat, trs({9.000f, 1.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_41", root, 56.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_41", root, LightKind::Point, 2.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_41", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 20.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_41") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_41";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0042() {
  using namespace knotwork;
  const std::string name = "generated_case_0042";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_42", {0.000f, 0.500f, 0.000f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_42", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_42", root, mat, trs({10.000f, 0.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_42", root, 57.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_42", root, LightKind::Point, 3.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_42", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 22.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_42") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_42";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0043() {
  using namespace knotwork;
  const std::string name = "generated_case_0043";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_43", {0.500f, 0.750f, 0.167f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_43", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_43", root, mat, trs({11.000f, 0.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_43", root, 58.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_43", root, LightKind::Point, 4.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_43", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 24.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_43") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_43";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0044() {
  using namespace knotwork;
  const std::string name = "generated_case_0044";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_44", {1.000f, 1.000f, 0.333f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_44", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_44", root, mat, trs({1.000f, 0.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_44", root, 59.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_44", root, LightKind::Point, 5.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_44", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 15.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_44") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_44";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0045() {
  using namespace knotwork;
  const std::string name = "generated_case_0045";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_45", {0.000f, 0.000f, 0.500f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_45", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_45", root, mat, trs({2.000f, 0.750f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_45", root, 60.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_45", root, LightKind::Point, 6.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_45", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 17.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_45") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_45";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0046() {
  using namespace knotwork;
  const std::string name = "generated_case_0046";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_46", {0.500f, 0.250f, 0.667f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_46", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_46", root, mat, trs({3.000f, 1.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_46", root, 61.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_46", root, LightKind::Point, 7.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_46", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 19.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_46") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_46";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0047() {
  using namespace knotwork;
  const std::string name = "generated_case_0047";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_47", {1.000f, 0.500f, 0.833f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_47", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_47", root, mat, trs({4.000f, 1.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_47", root, 62.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_47", root, LightKind::Point, 8.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_47", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 21.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_47") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_47";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0048() {
  using namespace knotwork;
  const std::string name = "generated_case_0048";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_48", {0.000f, 0.750f, 1.000f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_48", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_48", root, mat, trs({5.000f, 1.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_48", root, 63.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_48", root, LightKind::Point, 9.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_48", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 23.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_48") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_48";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0049() {
  using namespace knotwork;
  const std::string name = "generated_case_0049";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_49", {0.500f, 1.000f, 0.000f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_49", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_49", root, mat, trs({6.000f, 0.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_49", root, 64.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_49", root, LightKind::Point, 10.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_49", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 25.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_49") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_49";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0050() {
  using namespace knotwork;
  const std::string name = "generated_case_0050";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_50", {1.000f, 0.000f, 0.167f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_50", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_50", root, mat, trs({7.000f, 0.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_50", root, 65.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_50", root, LightKind::Point, 11.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_50", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 23.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 27.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_50") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({23.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_50";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 24.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0051() {
  using namespace knotwork;
  const std::string name = "generated_case_0051";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_51", {0.000f, 0.250f, 0.333f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_51", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_51", root, mat, trs({8.000f, 0.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_51", root, 66.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_51", root, LightKind::Point, 12.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_51", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_51") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_51";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0052() {
  using namespace knotwork;
  const std::string name = "generated_case_0052";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_52", {0.500f, 0.500f, 0.500f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_52", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_52", root, mat, trs({9.000f, 0.750f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_52", root, 67.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_52", root, LightKind::Point, 13.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_52", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 10.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_52") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_52";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0053() {
  using namespace knotwork;
  const std::string name = "generated_case_0053";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_53", {1.000f, 0.750f, 0.667f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_53", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_53", root, mat, trs({10.000f, 1.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_53", root, 68.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_53", root, LightKind::Point, 14.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_53", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 12.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_53") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_53";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0054() {
  using namespace knotwork;
  const std::string name = "generated_case_0054";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_54", {0.000f, 1.000f, 0.833f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_54", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_54", root, mat, trs({11.000f, 1.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_54", root, 69.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_54", root, LightKind::Point, 15.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_54", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 14.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_54") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_54";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0055() {
  using namespace knotwork;
  const std::string name = "generated_case_0055";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_55", {0.500f, 0.000f, 1.000f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_55", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_55", root, mat, trs({1.000f, 1.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_55", root, 70.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_55", root, LightKind::Point, 16.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_55", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 5.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_55") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({5.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_55";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 6.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0056() {
  using namespace knotwork;
  const std::string name = "generated_case_0056";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_56", {1.000f, 0.250f, 0.000f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_56", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_56", root, mat, trs({2.000f, 0.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_56", root, 71.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_56", root, LightKind::Point, 17.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_56", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_56") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_56";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0057() {
  using namespace knotwork;
  const std::string name = "generated_case_0057";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_57", {0.000f, 0.500f, 0.167f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_57", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_57", root, mat, trs({3.000f, 0.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_57", root, 72.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_57", root, LightKind::Point, 18.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_57", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_57") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_57";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0058() {
  using namespace knotwork;
  const std::string name = "generated_case_0058";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_58", {0.500f, 0.750f, 0.333f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_58", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_58", root, mat, trs({4.000f, 0.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_58", root, 73.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_58", root, LightKind::Point, 19.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_58", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 11.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_58") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_58";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0059() {
  using namespace knotwork;
  const std::string name = "generated_case_0059";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_59", {1.000f, 1.000f, 0.500f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_59", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_59", root, mat, trs({5.000f, 0.750f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_59", root, 74.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_59", root, LightKind::Point, 20.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_59", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 13.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_59") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_59";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0060() {
  using namespace knotwork;
  const std::string name = "generated_case_0060";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_60", {0.000f, 0.000f, 0.667f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_60", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_60", root, mat, trs({6.000f, 1.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_60", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_60", root, LightKind::Point, 1.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_60", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 15.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_60") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_60";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0061() {
  using namespace knotwork;
  const std::string name = "generated_case_0061";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_61", {0.500f, 0.250f, 0.833f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_61", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_61", root, mat, trs({7.000f, 1.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_61", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_61", root, LightKind::Point, 2.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_61", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 17.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_61") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_61";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0062() {
  using namespace knotwork;
  const std::string name = "generated_case_0062";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_62", {1.000f, 0.500f, 1.000f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_62", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_62", root, mat, trs({8.000f, 1.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_62", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_62", root, LightKind::Point, 3.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_62", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 19.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_62") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_62";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0063() {
  using namespace knotwork;
  const std::string name = "generated_case_0063";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_63", {0.000f, 0.750f, 0.000f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_63", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_63", root, mat, trs({9.000f, 0.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_63", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_63", root, LightKind::Point, 4.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_63", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 21.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_63") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_63";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0064() {
  using namespace knotwork;
  const std::string name = "generated_case_0064";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_64", {0.500f, 1.000f, 0.167f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_64", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_64", root, mat, trs({10.000f, 0.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_64", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_64", root, LightKind::Point, 5.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_64", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 23.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 23.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_64") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({23.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_64";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 24.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0065() {
  using namespace knotwork;
  const std::string name = "generated_case_0065";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_65", {1.000f, 0.000f, 0.333f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_65", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_65", root, mat, trs({11.000f, 0.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_65", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_65", root, LightKind::Point, 6.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_65", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 25.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 38.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_65") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({25.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_65";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 26.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0066() {
  using namespace knotwork;
  const std::string name = "generated_case_0066";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_66", {0.000f, 0.250f, 0.500f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_66", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_66", root, mat, trs({1.000f, 0.750f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_66", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_66", root, LightKind::Point, 7.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_66", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 29.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_66") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_66";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0067() {
  using namespace knotwork;
  const std::string name = "generated_case_0067";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_67", {0.500f, 0.500f, 0.667f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_67", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_67", root, mat, trs({2.000f, 1.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_67", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_67", root, LightKind::Point, 8.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_67", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 31.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_67") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_67";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0068() {
  using namespace knotwork;
  const std::string name = "generated_case_0068";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_68", {1.000f, 0.750f, 0.833f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_68", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_68", root, mat, trs({3.000f, 1.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_68", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_68", root, LightKind::Point, 9.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_68", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 3.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_68") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({3.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_68";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 4.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0069() {
  using namespace knotwork;
  const std::string name = "generated_case_0069";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_69", {0.000f, 1.000f, 1.000f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_69", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_69", root, mat, trs({4.000f, 1.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_69", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_69", root, LightKind::Point, 10.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_69", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 5.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_69") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({5.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_69";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 6.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0070() {
  using namespace knotwork;
  const std::string name = "generated_case_0070";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_70", {0.500f, 0.000f, 0.000f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_70", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_70", root, mat, trs({5.000f, 0.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_70", root, 55.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_70", root, LightKind::Point, 11.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_70", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_70") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_70";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0071() {
  using namespace knotwork;
  const std::string name = "generated_case_0071";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_71", {1.000f, 0.250f, 0.167f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_71", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_71", root, mat, trs({6.000f, 0.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_71", root, 56.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_71", root, LightKind::Point, 12.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_71", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_71") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_71";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0072() {
  using namespace knotwork;
  const std::string name = "generated_case_0072";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_72", {0.000f, 0.500f, 0.333f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_72", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_72", root, mat, trs({7.000f, 0.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_72", root, 57.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_72", root, LightKind::Point, 13.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_72", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_72") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_72";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0073() {
  using namespace knotwork;
  const std::string name = "generated_case_0073";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_73", {0.500f, 0.750f, 0.500f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_73", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_73", root, mat, trs({8.000f, 0.750f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_73", root, 58.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_73", root, LightKind::Point, 14.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_73", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_73") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_73";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0074() {
  using namespace knotwork;
  const std::string name = "generated_case_0074";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_74", {1.000f, 1.000f, 0.667f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_74", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_74", root, mat, trs({9.000f, 1.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_74", root, 59.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_74", root, LightKind::Point, 15.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_74", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 11.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_74") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_74";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0075() {
  using namespace knotwork;
  const std::string name = "generated_case_0075";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_75", {0.000f, 0.000f, 0.833f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_75", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_75", root, mat, trs({10.000f, 1.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_75", root, 60.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_75", root, LightKind::Point, 16.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_75", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 13.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_75") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_75";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0076() {
  using namespace knotwork;
  const std::string name = "generated_case_0076";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_76", {0.500f, 0.250f, 1.000f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_76", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_76", root, mat, trs({11.000f, 1.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_76", root, 61.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_76", root, LightKind::Point, 17.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_76", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 15.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_76") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_76";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0077() {
  using namespace knotwork;
  const std::string name = "generated_case_0077";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_77", {1.000f, 0.500f, 0.000f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_77", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_77", root, mat, trs({1.000f, 0.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_77", root, 62.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_77", root, LightKind::Point, 18.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_77", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 6.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_77") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_77";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0078() {
  using namespace knotwork;
  const std::string name = "generated_case_0078";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_78", {0.000f, 0.750f, 0.167f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_78", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_78", root, mat, trs({2.000f, 0.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_78", root, 63.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_78", root, LightKind::Point, 19.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_78", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 21.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_78") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_78";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0079() {
  using namespace knotwork;
  const std::string name = "generated_case_0079";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_79", {0.500f, 1.000f, 0.333f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_79", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_79", root, mat, trs({3.000f, 0.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_79", root, 64.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_79", root, LightKind::Point, 20.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_79", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 23.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_79") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_79";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0080() {
  using namespace knotwork;
  const std::string name = "generated_case_0080";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_80", {1.000f, 0.000f, 0.500f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_80", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_80", root, mat, trs({4.000f, 0.750f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_80", root, 65.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_80", root, LightKind::Point, 1.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_80", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 25.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_80") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_80";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0081() {
  using namespace knotwork;
  const std::string name = "generated_case_0081";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_81", {0.000f, 0.250f, 0.667f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_81", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_81", root, mat, trs({5.000f, 1.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_81", root, 66.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_81", root, LightKind::Point, 2.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_81", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 27.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_81") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_81";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0082() {
  using namespace knotwork;
  const std::string name = "generated_case_0082";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_82", {0.500f, 0.500f, 0.833f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_82", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_82", root, mat, trs({6.000f, 1.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_82", root, 67.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_82", root, LightKind::Point, 3.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_82", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 29.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_82") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_82";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0083() {
  using namespace knotwork;
  const std::string name = "generated_case_0083";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_83", {1.000f, 0.750f, 1.000f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_83", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_83", root, mat, trs({7.000f, 1.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_83", root, 68.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_83", root, LightKind::Point, 4.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_83", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 22.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 31.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_83") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({22.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_83";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 23.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0084() {
  using namespace knotwork;
  const std::string name = "generated_case_0084";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_84", {0.000f, 1.000f, 0.000f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_84", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_84", root, mat, trs({8.000f, 0.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_84", root, 69.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_84", root, LightKind::Point, 5.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_84", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 24.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 33.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_84") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({24.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_84";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 25.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0085() {
  using namespace knotwork;
  const std::string name = "generated_case_0085";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_85", {0.500f, 0.000f, 0.167f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_85", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_85", root, mat, trs({9.000f, 0.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_85", root, 70.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_85", root, LightKind::Point, 6.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_85", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_85") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_85";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0086() {
  using namespace knotwork;
  const std::string name = "generated_case_0086";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_86", {1.000f, 0.250f, 0.333f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_86", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_86", root, mat, trs({10.000f, 0.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_86", root, 71.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_86", root, LightKind::Point, 7.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_86", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_86") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_86";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0087() {
  using namespace knotwork;
  const std::string name = "generated_case_0087";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_87", {0.000f, 0.500f, 0.500f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_87", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_87", root, mat, trs({11.000f, 0.750f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_87", root, 72.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_87", root, LightKind::Point, 8.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_87", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_87") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_87";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0088() {
  using namespace knotwork;
  const std::string name = "generated_case_0088";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_88", {0.500f, 0.750f, 0.667f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_88", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_88", root, mat, trs({1.000f, 1.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_88", root, 73.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_88", root, LightKind::Point, 9.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_88", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 4.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_88") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({4.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_88";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 5.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0089() {
  using namespace knotwork;
  const std::string name = "generated_case_0089";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_89", {1.000f, 1.000f, 0.833f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_89", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_89", root, mat, trs({2.000f, 1.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_89", root, 74.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_89", root, LightKind::Point, 10.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_89", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 6.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -2.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_89") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({6.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_89";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 7.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0090() {
  using namespace knotwork;
  const std::string name = "generated_case_0090";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_90", {0.000f, 0.000f, 1.000f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_90", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_90", root, mat, trs({3.000f, 1.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_90", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_90", root, LightKind::Point, 11.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_90", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 0.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_90") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_90";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0091() {
  using namespace knotwork;
  const std::string name = "generated_case_0091";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_91", {0.500f, 0.250f, 0.000f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_91", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_91", root, mat, trs({4.000f, 0.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_91", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_91", root, LightKind::Point, 12.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_91", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 15.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_91") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_91";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0092() {
  using namespace knotwork;
  const std::string name = "generated_case_0092";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_92", {1.000f, 0.500f, 0.167f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_92", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_92", root, mat, trs({5.000f, 0.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_92", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_92", root, LightKind::Point, 13.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_92", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 17.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_92") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_92";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0093() {
  using namespace knotwork;
  const std::string name = "generated_case_0093";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_93", {0.000f, 0.750f, 0.333f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_93", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_93", root, mat, trs({6.000f, 0.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_93", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_93", root, LightKind::Point, 14.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_93", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 19.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_93") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_93";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0094() {
  using namespace knotwork;
  const std::string name = "generated_case_0094";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_94", {0.500f, 1.000f, 0.500f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_94", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_94", root, mat, trs({7.000f, 0.750f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_94", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_94", root, LightKind::Point, 15.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_94", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 21.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_94") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_94";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0095() {
  using namespace knotwork;
  const std::string name = "generated_case_0095";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_95", {1.000f, 0.000f, 0.667f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_95", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_95", root, mat, trs({8.000f, 1.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_95", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_95", root, LightKind::Point, 16.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_95", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 23.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_95") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_95";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0096() {
  using namespace knotwork;
  const std::string name = "generated_case_0096";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_96", {0.000f, 0.250f, 0.833f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_96", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_96", root, mat, trs({9.000f, 1.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_96", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_96", root, LightKind::Point, 17.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_96", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 25.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_96") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_96";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0097() {
  using namespace knotwork;
  const std::string name = "generated_case_0097";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_97", {0.500f, 0.500f, 1.000f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_97", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_97", root, mat, trs({10.000f, 1.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_97", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_97", root, LightKind::Point, 18.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_97", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 22.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 27.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_97") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({22.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_97";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 23.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0098() {
  using namespace knotwork;
  const std::string name = "generated_case_0098";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_98", {1.000f, 0.750f, 0.000f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_98", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_98", root, mat, trs({11.000f, 0.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_98", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_98", root, LightKind::Point, 19.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_98", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 24.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 29.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_98") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({24.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_98";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 25.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0099() {
  using namespace knotwork;
  const std::string name = "generated_case_0099";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_99", {0.000f, 1.000f, 0.167f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_99", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_99", root, mat, trs({1.000f, 0.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_99", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_99", root, LightKind::Point, 20.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_99", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 20.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_99") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_99";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0100() {
  using namespace knotwork;
  const std::string name = "generated_case_0100";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_100", {0.500f, 0.000f, 0.333f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_100", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_100", root, mat, trs({2.000f, 0.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_100", root, 55.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_100", root, LightKind::Point, 1.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_100", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 22.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_100") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_100";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0101() {
  using namespace knotwork;
  const std::string name = "generated_case_0101";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_101", {1.000f, 0.250f, 0.500f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_101", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_101", root, mat, trs({3.000f, 0.750f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_101", root, 56.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_101", root, LightKind::Point, 2.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_101", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 24.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_101") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_101";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0102() {
  using namespace knotwork;
  const std::string name = "generated_case_0102";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_102", {0.000f, 0.500f, 0.667f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_102", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_102", root, mat, trs({4.000f, 1.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_102", root, 57.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_102", root, LightKind::Point, 3.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_102", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 4.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_102") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({4.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_102";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 5.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0103() {
  using namespace knotwork;
  const std::string name = "generated_case_0103";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_103", {0.500f, 0.750f, 0.833f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_103", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_103", root, mat, trs({5.000f, 1.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_103", root, 58.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_103", root, LightKind::Point, 4.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_103", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 6.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -6.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_103") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({6.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_103";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 7.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0104() {
  using namespace knotwork;
  const std::string name = "generated_case_0104";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_104", {1.000f, 1.000f, 1.000f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_104", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_104", root, mat, trs({6.000f, 1.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_104", root, 59.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_104", root, LightKind::Point, 5.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_104", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_104") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_104";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0105() {
  using namespace knotwork;
  const std::string name = "generated_case_0105";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_105", {0.000f, 0.000f, 0.000f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_105", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_105", root, mat, trs({7.000f, 0.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_105", root, 60.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_105", root, LightKind::Point, 6.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_105", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 11.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_105") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_105";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0106() {
  using namespace knotwork;
  const std::string name = "generated_case_0106";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_106", {0.500f, 0.250f, 0.167f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_106", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_106", root, mat, trs({8.000f, 0.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_106", root, 61.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_106", root, LightKind::Point, 7.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_106", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 13.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_106") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_106";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0107() {
  using namespace knotwork;
  const std::string name = "generated_case_0107";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_107", {1.000f, 0.500f, 0.333f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_107", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_107", root, mat, trs({9.000f, 0.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_107", root, 62.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_107", root, LightKind::Point, 8.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_107", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 15.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_107") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_107";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0108() {
  using namespace knotwork;
  const std::string name = "generated_case_0108";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_108", {0.000f, 0.750f, 0.500f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_108", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_108", root, mat, trs({10.000f, 0.750f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_108", root, 63.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_108", root, LightKind::Point, 9.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_108", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 17.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_108") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_108";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0109() {
  using namespace knotwork;
  const std::string name = "generated_case_0109";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_109", {0.500f, 1.000f, 0.667f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_109", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_109", root, mat, trs({11.000f, 1.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_109", root, 64.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_109", root, LightKind::Point, 10.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_109", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 19.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_109") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_109";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0110() {
  using namespace knotwork;
  const std::string name = "generated_case_0110";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_110", {1.000f, 0.000f, 0.833f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_110", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_110", root, mat, trs({1.000f, 1.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_110", root, 65.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_110", root, LightKind::Point, 11.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_110", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 10.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_110") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_110";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0111() {
  using namespace knotwork;
  const std::string name = "generated_case_0111";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_111", {0.000f, 0.250f, 1.000f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_111", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_111", root, mat, trs({2.000f, 1.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_111", root, 66.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_111", root, LightKind::Point, 12.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_111", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 12.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_111") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_111";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0112() {
  using namespace knotwork;
  const std::string name = "generated_case_0112";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_112", {0.500f, 0.500f, 0.000f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_112", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_112", root, mat, trs({3.000f, 0.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_112", root, 67.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_112", root, LightKind::Point, 13.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_112", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 14.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_112") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_112";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0113() {
  using namespace knotwork;
  const std::string name = "generated_case_0113";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_113", {1.000f, 0.750f, 0.167f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_113", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_113", root, mat, trs({4.000f, 0.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_113", root, 68.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_113", root, LightKind::Point, 14.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_113", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 16.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_113") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_113";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0114() {
  using namespace knotwork;
  const std::string name = "generated_case_0114";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_114", {0.000f, 1.000f, 0.333f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_114", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_114", root, mat, trs({5.000f, 0.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_114", root, 69.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_114", root, LightKind::Point, 15.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_114", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 18.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_114") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_114";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0115() {
  using namespace knotwork;
  const std::string name = "generated_case_0115";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_115", {0.500f, 0.000f, 0.500f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_115", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_115", root, mat, trs({6.000f, 0.750f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_115", root, 70.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_115", root, LightKind::Point, 16.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_115", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 20.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_115") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_115";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0116() {
  using namespace knotwork;
  const std::string name = "generated_case_0116";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_116", {1.000f, 0.250f, 0.667f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_116", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_116", root, mat, trs({7.000f, 1.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_116", root, 71.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_116", root, LightKind::Point, 17.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_116", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 22.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_116") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_116";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0117() {
  using namespace knotwork;
  const std::string name = "generated_case_0117";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_117", {0.000f, 0.500f, 0.833f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_117", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_117", root, mat, trs({8.000f, 1.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_117", root, 72.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_117", root, LightKind::Point, 18.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_117", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 23.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 37.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_117") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({23.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_117";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 24.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0118() {
  using namespace knotwork;
  const std::string name = "generated_case_0118";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_118", {0.500f, 0.750f, 1.000f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_118", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_118", root, mat, trs({9.000f, 1.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_118", root, 73.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_118", root, LightKind::Point, 19.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_118", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 25.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 39.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_118") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({25.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_118";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 26.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0119() {
  using namespace knotwork;
  const std::string name = "generated_case_0119";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_119", {1.000f, 1.000f, 0.000f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_119", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_119", root, mat, trs({10.000f, 0.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_119", root, 74.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_119", root, LightKind::Point, 20.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_119", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_119") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_119";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0120() {
  using namespace knotwork;
  const std::string name = "generated_case_0120";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_120", {0.000f, 0.000f, 0.167f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_120", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_120", root, mat, trs({11.000f, 0.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_120", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_120", root, LightKind::Point, 1.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_120", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_120") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_120";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0121() {
  using namespace knotwork;
  const std::string name = "generated_case_0121";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_121", {0.500f, 0.250f, 0.333f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_121", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_121", root, mat, trs({1.000f, 0.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_121", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_121", root, LightKind::Point, 2.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_121", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 3.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 0.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_121") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({3.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_121";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 4.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0122() {
  using namespace knotwork;
  const std::string name = "generated_case_0122";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_122", {1.000f, 0.500f, 0.500f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_122", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_122", root, mat, trs({2.000f, 0.750f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_122", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_122", root, LightKind::Point, 3.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_122", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 5.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 2.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_122") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({5.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_122";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 6.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0123() {
  using namespace knotwork;
  const std::string name = "generated_case_0123";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_123", {0.000f, 0.750f, 0.667f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_123", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_123", root, mat, trs({3.000f, 1.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_123", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_123", root, LightKind::Point, 4.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_123", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_123") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_123";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0124() {
  using namespace knotwork;
  const std::string name = "generated_case_0124";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_124", {0.500f, 1.000f, 0.833f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_124", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_124", root, mat, trs({4.000f, 1.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_124", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_124", root, LightKind::Point, 5.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_124", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 6.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_124") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_124";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0125() {
  using namespace knotwork;
  const std::string name = "generated_case_0125";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_125", {1.000f, 0.000f, 1.000f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_125", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_125", root, mat, trs({5.000f, 1.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_125", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_125", root, LightKind::Point, 6.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_125", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_125") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_125";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0126() {
  using namespace knotwork;
  const std::string name = "generated_case_0126";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_126", {0.000f, 0.250f, 0.000f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_126", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_126", root, mat, trs({6.000f, 0.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_126", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_126", root, LightKind::Point, 7.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_126", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 10.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_126") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_126";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0127() {
  using namespace knotwork;
  const std::string name = "generated_case_0127";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_127", {0.500f, 0.500f, 0.167f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_127", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_127", root, mat, trs({7.000f, 0.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_127", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_127", root, LightKind::Point, 8.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_127", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 12.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_127") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_127";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0128() {
  using namespace knotwork;
  const std::string name = "generated_case_0128";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_128", {1.000f, 0.750f, 0.333f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_128", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_128", root, mat, trs({8.000f, 0.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_128", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_128", root, LightKind::Point, 9.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_128", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 14.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_128") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_128";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0129() {
  using namespace knotwork;
  const std::string name = "generated_case_0129";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_129", {0.000f, 1.000f, 0.500f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_129", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_129", root, mat, trs({9.000f, 0.750f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_129", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_129", root, LightKind::Point, 10.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_129", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 16.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_129") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_129";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0130() {
  using namespace knotwork;
  const std::string name = "generated_case_0130";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_130", {0.500f, 0.000f, 0.667f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_130", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_130", root, mat, trs({10.000f, 1.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_130", root, 55.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_130", root, LightKind::Point, 11.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_130", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 31.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_130") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_130";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0131() {
  using namespace knotwork;
  const std::string name = "generated_case_0131";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_131", {1.000f, 0.250f, 0.833f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_131", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_131", root, mat, trs({11.000f, 1.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_131", root, 56.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_131", root, LightKind::Point, 12.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_131", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 23.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 33.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_131") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({23.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_131";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 24.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0132() {
  using namespace knotwork;
  const std::string name = "generated_case_0132";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_132", {0.000f, 0.500f, 1.000f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_132", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_132", root, mat, trs({1.000f, 1.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_132", root, 57.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_132", root, LightKind::Point, 13.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_132", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 24.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_132") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_132";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0133() {
  using namespace knotwork;
  const std::string name = "generated_case_0133";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_133", {0.500f, 0.750f, 0.000f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_133", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_133", root, mat, trs({2.000f, 0.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_133", root, 58.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_133", root, LightKind::Point, 14.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_133", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 26.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_133") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_133";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0134() {
  using namespace knotwork;
  const std::string name = "generated_case_0134";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_134", {1.000f, 1.000f, 0.167f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_134", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_134", root, mat, trs({3.000f, 0.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_134", root, 59.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_134", root, LightKind::Point, 15.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_134", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 28.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_134") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_134";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0135() {
  using namespace knotwork;
  const std::string name = "generated_case_0135";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_135", {0.000f, 0.000f, 0.333f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_135", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_135", root, mat, trs({4.000f, 0.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_135", root, 60.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_135", root, LightKind::Point, 16.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_135", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 30.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_135") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_135";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0136() {
  using namespace knotwork;
  const std::string name = "generated_case_0136";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_136", {0.500f, 0.250f, 0.500f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_136", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_136", root, mat, trs({5.000f, 0.750f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_136", root, 61.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_136", root, LightKind::Point, 17.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_136", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 5.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -2.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_136") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({5.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_136";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 6.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0137() {
  using namespace knotwork;
  const std::string name = "generated_case_0137";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_137", {1.000f, 0.500f, 0.667f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_137", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_137", root, mat, trs({6.000f, 1.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_137", root, 62.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_137", root, LightKind::Point, 18.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_137", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 0.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_137") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_137";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0138() {
  using namespace knotwork;
  const std::string name = "generated_case_0138";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_138", {0.000f, 0.750f, 0.833f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_138", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_138", root, mat, trs({7.000f, 1.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_138", root, 63.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_138", root, LightKind::Point, 19.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_138", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 2.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_138") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_138";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0139() {
  using namespace knotwork;
  const std::string name = "generated_case_0139";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_139", {0.500f, 1.000f, 1.000f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_139", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_139", root, mat, trs({8.000f, 1.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_139", root, 64.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_139", root, LightKind::Point, 20.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_139", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_139") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_139";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0140() {
  using namespace knotwork;
  const std::string name = "generated_case_0140";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_140", {1.000f, 0.000f, 0.000f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_140", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_140", root, mat, trs({9.000f, 0.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_140", root, 65.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_140", root, LightKind::Point, 1.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_140", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 6.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_140") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_140";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0141() {
  using namespace knotwork;
  const std::string name = "generated_case_0141";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_141", {0.000f, 0.250f, 0.167f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_141", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_141", root, mat, trs({10.000f, 0.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_141", root, 66.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_141", root, LightKind::Point, 2.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_141", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_141") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_141";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0142() {
  using namespace knotwork;
  const std::string name = "generated_case_0142";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_142", {0.500f, 0.500f, 0.333f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_142", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_142", root, mat, trs({11.000f, 0.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_142", root, 67.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_142", root, LightKind::Point, 3.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_142", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 10.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_142") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_142";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0143() {
  using namespace knotwork;
  const std::string name = "generated_case_0143";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_143", {1.000f, 0.750f, 0.500f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_143", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_143", root, mat, trs({1.000f, 0.750f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_143", root, 68.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_143", root, LightKind::Point, 4.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_143", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 14.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_143") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_143";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0144() {
  using namespace knotwork;
  const std::string name = "generated_case_0144";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_144", {0.000f, 1.000f, 0.667f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_144", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_144", root, mat, trs({2.000f, 1.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_144", root, 69.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_144", root, LightKind::Point, 5.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_144", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 16.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_144") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_144";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0145() {
  using namespace knotwork;
  const std::string name = "generated_case_0145";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_145", {0.500f, 0.000f, 0.833f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_145", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_145", root, mat, trs({3.000f, 1.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_145", root, 70.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_145", root, LightKind::Point, 6.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_145", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 18.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_145") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_145";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0146() {
  using namespace knotwork;
  const std::string name = "generated_case_0146";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_146", {1.000f, 0.250f, 1.000f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_146", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_146", root, mat, trs({4.000f, 1.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_146", root, 71.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_146", root, LightKind::Point, 7.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_146", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 20.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_146") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_146";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0147() {
  using namespace knotwork;
  const std::string name = "generated_case_0147";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_147", {0.000f, 0.500f, 0.000f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_147", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_147", root, mat, trs({5.000f, 0.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_147", root, 72.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_147", root, LightKind::Point, 8.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_147", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 22.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_147") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_147";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0148() {
  using namespace knotwork;
  const std::string name = "generated_case_0148";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_148", {0.500f, 0.750f, 0.167f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_148", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_148", root, mat, trs({6.000f, 0.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_148", root, 73.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_148", root, LightKind::Point, 9.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_148", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 24.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_148") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_148";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0149() {
  using namespace knotwork;
  const std::string name = "generated_case_0149";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_149", {1.000f, 1.000f, 0.333f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_149", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_149", root, mat, trs({7.000f, 0.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_149", root, 74.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_149", root, LightKind::Point, 10.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_149", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 26.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_149") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_149";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0150() {
  using namespace knotwork;
  const std::string name = "generated_case_0150";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_150", {0.000f, 0.000f, 0.500f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_150", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_150", root, mat, trs({8.000f, 0.750f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_150", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_150", root, LightKind::Point, 11.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_150", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 22.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 28.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_150") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({22.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_150";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 23.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0151() {
  using namespace knotwork;
  const std::string name = "generated_case_0151";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_151", {0.500f, 0.250f, 0.667f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_151", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_151", root, mat, trs({9.000f, 1.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_151", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_151", root, LightKind::Point, 12.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_151", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 24.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 30.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_151") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({24.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_151";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 25.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0152() {
  using namespace knotwork;
  const std::string name = "generated_case_0152";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_152", {1.000f, 0.500f, 0.833f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_152", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_152", root, mat, trs({10.000f, 1.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_152", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_152", root, LightKind::Point, 13.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_152", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 26.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 32.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_152") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({26.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_152";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 27.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0153() {
  using namespace knotwork;
  const std::string name = "generated_case_0153";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_153", {0.000f, 0.750f, 1.000f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_153", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_153", root, mat, trs({11.000f, 1.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_153", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_153", root, LightKind::Point, 14.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_153", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 0.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_153") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_153";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0154() {
  using namespace knotwork;
  const std::string name = "generated_case_0154";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_154", {0.500f, 1.000f, 0.000f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_154", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_154", root, mat, trs({1.000f, 0.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_154", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_154", root, LightKind::Point, 15.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_154", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 2.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_154") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({2.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_154";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 3.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0155() {
  using namespace knotwork;
  const std::string name = "generated_case_0155";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_155", {1.000f, 0.000f, 0.167f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_155", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_155", root, mat, trs({2.000f, 0.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_155", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_155", root, LightKind::Point, 16.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_155", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 4.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_155") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({4.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_155";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 5.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0156() {
  using namespace knotwork;
  const std::string name = "generated_case_0156";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_156", {0.000f, 0.250f, 0.333f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_156", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_156", root, mat, trs({3.000f, 0.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_156", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_156", root, LightKind::Point, 17.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_156", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 6.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_156") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({6.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_156";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 7.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0157() {
  using namespace knotwork;
  const std::string name = "generated_case_0157";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_157", {0.500f, 0.500f, 0.500f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_157", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_157", root, mat, trs({4.000f, 0.750f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_157", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_157", root, LightKind::Point, 18.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_157", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 10.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_157") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_157";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0158() {
  using namespace knotwork;
  const std::string name = "generated_case_0158";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_158", {1.000f, 0.750f, 0.667f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_158", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_158", root, mat, trs({5.000f, 1.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_158", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_158", root, LightKind::Point, 19.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_158", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 12.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_158") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_158";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0159() {
  using namespace knotwork;
  const std::string name = "generated_case_0159";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_159", {0.000f, 1.000f, 0.833f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_159", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_159", root, mat, trs({6.000f, 1.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_159", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_159", root, LightKind::Point, 20.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_159", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 14.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_159") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_159";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0160() {
  using namespace knotwork;
  const std::string name = "generated_case_0160";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_160", {0.500f, 0.000f, 1.000f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_160", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_160", root, mat, trs({7.000f, 1.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_160", root, 55.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_160", root, LightKind::Point, 1.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_160", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 16.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_160") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_160";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0161() {
  using namespace knotwork;
  const std::string name = "generated_case_0161";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_161", {1.000f, 0.250f, 0.000f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_161", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_161", root, mat, trs({8.000f, 0.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_161", root, 56.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_161", root, LightKind::Point, 2.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_161", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 18.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_161") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_161";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0162() {
  using namespace knotwork;
  const std::string name = "generated_case_0162";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_162", {0.000f, 0.500f, 0.167f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_162", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_162", root, mat, trs({9.000f, 0.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_162", root, 57.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_162", root, LightKind::Point, 3.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_162", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 20.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_162") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_162";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0163() {
  using namespace knotwork;
  const std::string name = "generated_case_0163";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_163", {0.500f, 0.750f, 0.333f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_163", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_163", root, mat, trs({10.000f, 0.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_163", root, 58.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_163", root, LightKind::Point, 4.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_163", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 22.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_163") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_163";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0164() {
  using namespace knotwork;
  const std::string name = "generated_case_0164";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_164", {1.000f, 1.000f, 0.500f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_164", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_164", root, mat, trs({11.000f, 0.750f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_164", root, 59.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_164", root, LightKind::Point, 5.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_164", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 22.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 24.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_164") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({22.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_164";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 23.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0165() {
  using namespace knotwork;
  const std::string name = "generated_case_0165";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_165", {0.000f, 0.000f, 0.667f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_165", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_165", root, mat, trs({1.000f, 1.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_165", root, 60.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_165", root, LightKind::Point, 6.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_165", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 15.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_165") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_165";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0166() {
  using namespace knotwork;
  const std::string name = "generated_case_0166";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_166", {0.500f, 0.250f, 0.833f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_166", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_166", root, mat, trs({2.000f, 1.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_166", root, 61.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_166", root, LightKind::Point, 7.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_166", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 17.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_166") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_166";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0167() {
  using namespace knotwork;
  const std::string name = "generated_case_0167";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_167", {1.000f, 0.500f, 1.000f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_167", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_167", root, mat, trs({3.000f, 1.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_167", root, 62.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_167", root, LightKind::Point, 8.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_167", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 19.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_167") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_167";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0168() {
  using namespace knotwork;
  const std::string name = "generated_case_0168";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_168", {0.000f, 0.750f, 0.000f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_168", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_168", root, mat, trs({4.000f, 0.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_168", root, 63.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_168", root, LightKind::Point, 9.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_168", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 21.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_168") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_168";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0169() {
  using namespace knotwork;
  const std::string name = "generated_case_0169";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_169", {0.500f, 1.000f, 0.167f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_169", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_169", root, mat, trs({5.000f, 0.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_169", root, 64.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_169", root, LightKind::Point, 10.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_169", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 36.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_169") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_169";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0170() {
  using namespace knotwork;
  const std::string name = "generated_case_0170";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_170", {1.000f, 0.000f, 0.333f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_170", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_170", root, mat, trs({6.000f, 0.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_170", root, 65.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_170", root, LightKind::Point, 11.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_170", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 6.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_170") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({6.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_170";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 7.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0171() {
  using namespace knotwork;
  const std::string name = "generated_case_0171";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_171", {0.000f, 0.250f, 0.500f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_171", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_171", root, mat, trs({7.000f, 0.750f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_171", root, 66.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_171", root, LightKind::Point, 12.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_171", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 6.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_171") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_171";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0172() {
  using namespace knotwork;
  const std::string name = "generated_case_0172";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_172", {0.500f, 0.500f, 0.667f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_172", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_172", root, mat, trs({8.000f, 1.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_172", root, 67.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_172", root, LightKind::Point, 13.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_172", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_172") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_172";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0173() {
  using namespace knotwork;
  const std::string name = "generated_case_0173";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_173", {1.000f, 0.750f, 0.833f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_173", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_173", root, mat, trs({9.000f, 1.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_173", root, 68.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_173", root, LightKind::Point, 14.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_173", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 10.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_173") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_173";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0174() {
  using namespace knotwork;
  const std::string name = "generated_case_0174";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_174", {0.000f, 1.000f, 1.000f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_174", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_174", root, mat, trs({10.000f, 1.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_174", root, 69.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_174", root, LightKind::Point, 15.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_174", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 12.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_174") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_174";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0175() {
  using namespace knotwork;
  const std::string name = "generated_case_0175";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_175", {0.500f, 0.000f, 0.000f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_175", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_175", root, mat, trs({11.000f, 0.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_175", root, 70.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_175", root, LightKind::Point, 16.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_175", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 14.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_175") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_175";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0176() {
  using namespace knotwork;
  const std::string name = "generated_case_0176";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_176", {1.000f, 0.250f, 0.167f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_176", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_176", root, mat, trs({1.000f, 0.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_176", root, 71.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_176", root, LightKind::Point, 17.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_176", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_176") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_176";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0177() {
  using namespace knotwork;
  const std::string name = "generated_case_0177";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_177", {0.000f, 0.500f, 0.333f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_177", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_177", root, mat, trs({2.000f, 0.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_177", root, 72.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_177", root, LightKind::Point, 18.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_177", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_177") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_177";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0178() {
  using namespace knotwork;
  const std::string name = "generated_case_0178";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_178", {0.500f, 0.750f, 0.500f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_178", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_178", root, mat, trs({3.000f, 0.750f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_178", root, 73.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_178", root, LightKind::Point, 19.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_178", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_178") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_178";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0179() {
  using namespace knotwork;
  const std::string name = "generated_case_0179";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_179", {1.000f, 1.000f, 0.667f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_179", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_179", root, mat, trs({4.000f, 1.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_179", root, 74.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_179", root, LightKind::Point, 20.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_179", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 11.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_179") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_179";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0180() {
  using namespace knotwork;
  const std::string name = "generated_case_0180";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_180", {0.000f, 0.000f, 0.833f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_180", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_180", root, mat, trs({5.000f, 1.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_180", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_180", root, LightKind::Point, 1.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_180", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 13.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_180") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_180";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0181() {
  using namespace knotwork;
  const std::string name = "generated_case_0181";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_181", {0.500f, 0.250f, 1.000f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_181", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_181", root, mat, trs({6.000f, 1.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_181", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_181", root, LightKind::Point, 2.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_181", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 15.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_181") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_181";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0182() {
  using namespace knotwork;
  const std::string name = "generated_case_0182";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_182", {1.000f, 0.500f, 0.000f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_182", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_182", root, mat, trs({7.000f, 0.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_182", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_182", root, LightKind::Point, 3.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_182", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 30.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_182") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_182";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0183() {
  using namespace knotwork;
  const std::string name = "generated_case_0183";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_183", {0.000f, 0.750f, 0.167f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_183", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_183", root, mat, trs({8.000f, 0.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_183", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_183", root, LightKind::Point, 4.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_183", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 32.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_183") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_183";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0184() {
  using namespace knotwork;
  const std::string name = "generated_case_0184";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_184", {0.500f, 1.000f, 0.333f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_184", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_184", root, mat, trs({9.000f, 0.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_184", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_184", root, LightKind::Point, 5.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_184", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 23.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 34.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_184") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({23.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_184";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 24.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0185() {
  using namespace knotwork;
  const std::string name = "generated_case_0185";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_185", {1.000f, 0.000f, 0.500f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_185", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_185", root, mat, trs({10.000f, 0.750f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_185", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_185", root, LightKind::Point, 6.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_185", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 25.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 36.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_185") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({25.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_185";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 26.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0186() {
  using namespace knotwork;
  const std::string name = "generated_case_0186";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_186", {0.000f, 0.250f, 0.667f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_186", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_186", root, mat, trs({11.000f, 1.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_186", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_186", root, LightKind::Point, 7.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_186", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 27.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 38.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_186") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({27.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_186";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 28.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0187() {
  using namespace knotwork;
  const std::string name = "generated_case_0187";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_187", {0.500f, 0.500f, 0.833f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_187", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_187", root, mat, trs({1.000f, 1.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_187", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_187", root, LightKind::Point, 8.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_187", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 1.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_187") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({1.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_187";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 2.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0188() {
  using namespace knotwork;
  const std::string name = "generated_case_0188";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_188", {1.000f, 0.750f, 1.000f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_188", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_188", root, mat, trs({2.000f, 1.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_188", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_188", root, LightKind::Point, 9.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_188", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 3.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_188") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({3.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_188";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 4.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0189() {
  using namespace knotwork;
  const std::string name = "generated_case_0189";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_189", {0.000f, 1.000f, 0.000f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_189", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_189", root, mat, trs({3.000f, 0.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_189", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_189", root, LightKind::Point, 10.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_189", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 5.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_189") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({5.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_189";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 6.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0190() {
  using namespace knotwork;
  const std::string name = "generated_case_0190";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_190", {0.500f, 0.000f, 0.167f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_190", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_190", root, mat, trs({4.000f, 0.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_190", root, 55.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_190", root, LightKind::Point, 11.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_190", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_190") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_190";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0191() {
  using namespace knotwork;
  const std::string name = "generated_case_0191";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_191", {1.000f, 0.250f, 0.333f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_191", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_191", root, mat, trs({5.000f, 0.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_191", root, 56.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_191", root, LightKind::Point, 12.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_191", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_191") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_191";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0192() {
  using namespace knotwork;
  const std::string name = "generated_case_0192";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_192", {0.000f, 0.500f, 0.500f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_192", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_192", root, mat, trs({6.000f, 0.750f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_192", root, 57.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_192", root, LightKind::Point, 13.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_192", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_192") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_192";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0193() {
  using namespace knotwork;
  const std::string name = "generated_case_0193";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_193", {0.500f, 0.750f, 0.667f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_193", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_193", root, mat, trs({7.000f, 1.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_193", root, 58.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_193", root, LightKind::Point, 14.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_193", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_193") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_193";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0194() {
  using namespace knotwork;
  const std::string name = "generated_case_0194";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_194", {1.000f, 1.000f, 0.833f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_194", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_194", root, mat, trs({8.000f, 1.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_194", root, 59.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_194", root, LightKind::Point, 15.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_194", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_194") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_194";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0195() {
  using namespace knotwork;
  const std::string name = "generated_case_0195";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_195", {0.000f, 0.000f, 1.000f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_195", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_195", root, mat, trs({9.000f, 1.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_195", root, 60.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_195", root, LightKind::Point, 16.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_195", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 24.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_195") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_195";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0196() {
  using namespace knotwork;
  const std::string name = "generated_case_0196";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_196", {0.500f, 0.250f, 0.000f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_196", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_196", root, mat, trs({10.000f, 0.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_196", root, 61.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_196", root, LightKind::Point, 17.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_196", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 26.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_196") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_196";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0197() {
  using namespace knotwork;
  const std::string name = "generated_case_0197";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_197", {1.000f, 0.500f, 0.167f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_197", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_197", root, mat, trs({11.000f, 0.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_197", root, 62.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_197", root, LightKind::Point, 18.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_197", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 28.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_197") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_197";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0198() {
  using namespace knotwork;
  const std::string name = "generated_case_0198";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_198", {0.000f, 0.750f, 0.333f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_198", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_198", root, mat, trs({1.000f, 0.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_198", root, 63.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_198", root, LightKind::Point, 19.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_198", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 19.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_198") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_198";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0199() {
  using namespace knotwork;
  const std::string name = "generated_case_0199";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_199", {0.500f, 1.000f, 0.500f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_199", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_199", root, mat, trs({2.000f, 0.750f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_199", root, 64.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_199", root, LightKind::Point, 20.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_199", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 21.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_199") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_199";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0200() {
  using namespace knotwork;
  const std::string name = "generated_case_0200";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_200", {1.000f, 0.000f, 0.667f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_200", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_200", root, mat, trs({3.000f, 1.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_200", root, 65.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_200", root, LightKind::Point, 1.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_200", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 23.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_200") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_200";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0201() {
  using namespace knotwork;
  const std::string name = "generated_case_0201";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_201", {0.000f, 0.250f, 0.833f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_201", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_201", root, mat, trs({4.000f, 1.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_201", root, 66.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_201", root, LightKind::Point, 2.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_201", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 25.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_201") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_201";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0202() {
  using namespace knotwork;
  const std::string name = "generated_case_0202";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_202", {0.500f, 0.500f, 1.000f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_202", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_202", root, mat, trs({5.000f, 1.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_202", root, 67.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_202", root, LightKind::Point, 3.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_202", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 27.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_202") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_202";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0203() {
  using namespace knotwork;
  const std::string name = "generated_case_0203";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_203", {1.000f, 0.750f, 0.000f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_203", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_203", root, mat, trs({6.000f, 0.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_203", root, 68.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_203", root, LightKind::Point, 4.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_203", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 22.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 29.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_203") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({22.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_203";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 23.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0204() {
  using namespace knotwork;
  const std::string name = "generated_case_0204";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_204", {0.000f, 1.000f, 0.167f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_204", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_204", root, mat, trs({7.000f, 0.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_204", root, 69.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_204", root, LightKind::Point, 5.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_204", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_204") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_204";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0205() {
  using namespace knotwork;
  const std::string name = "generated_case_0205";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_205", {0.500f, 0.000f, 0.333f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_205", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_205", root, mat, trs({8.000f, 0.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_205", root, 70.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_205", root, LightKind::Point, 6.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_205", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_205") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_205";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0206() {
  using namespace knotwork;
  const std::string name = "generated_case_0206";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_206", {1.000f, 0.250f, 0.500f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_206", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_206", root, mat, trs({9.000f, 0.750f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_206", root, 71.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_206", root, LightKind::Point, 7.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_206", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_206") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_206";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0207() {
  using namespace knotwork;
  const std::string name = "generated_case_0207";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_207", {0.000f, 0.500f, 0.667f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_207", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_207", root, mat, trs({10.000f, 1.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_207", root, 72.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_207", root, LightKind::Point, 8.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_207", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_207") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_207";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0208() {
  using namespace knotwork;
  const std::string name = "generated_case_0208";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_208", {0.500f, 0.750f, 0.833f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_208", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_208", root, mat, trs({11.000f, 1.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_208", root, 73.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_208", root, LightKind::Point, 9.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_208", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 18.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_208") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_208";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0209() {
  using namespace knotwork;
  const std::string name = "generated_case_0209";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_209", {1.000f, 1.000f, 1.000f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_209", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_209", root, mat, trs({1.000f, 1.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_209", root, 74.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_209", root, LightKind::Point, 10.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_209", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 6.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_209") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({6.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_209";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 7.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0210() {
  using namespace knotwork;
  const std::string name = "generated_case_0210";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_210", {0.000f, 0.000f, 0.000f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_210", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_210", root, mat, trs({2.000f, 0.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_210", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_210", root, LightKind::Point, 11.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_210", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 11.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_210") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_210";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0211() {
  using namespace knotwork;
  const std::string name = "generated_case_0211";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_211", {0.500f, 0.250f, 0.167f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_211", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_211", root, mat, trs({3.000f, 0.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_211", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_211", root, LightKind::Point, 12.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_211", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 13.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_211") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_211";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0212() {
  using namespace knotwork;
  const std::string name = "generated_case_0212";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_212", {1.000f, 0.500f, 0.333f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_212", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_212", root, mat, trs({4.000f, 0.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_212", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_212", root, LightKind::Point, 13.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_212", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 15.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_212") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_212";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0213() {
  using namespace knotwork;
  const std::string name = "generated_case_0213";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_213", {0.000f, 0.750f, 0.500f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_213", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_213", root, mat, trs({5.000f, 0.750f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_213", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_213", root, LightKind::Point, 14.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_213", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 17.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_213") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_213";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0214() {
  using namespace knotwork;
  const std::string name = "generated_case_0214";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_214", {0.500f, 1.000f, 0.667f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_214", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_214", root, mat, trs({6.000f, 1.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_214", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_214", root, LightKind::Point, 15.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_214", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 19.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_214") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_214";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0215() {
  using namespace knotwork;
  const std::string name = "generated_case_0215";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_215", {1.000f, 0.000f, 0.833f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_215", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_215", root, mat, trs({7.000f, 1.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_215", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_215", root, LightKind::Point, 16.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_215", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 21.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_215") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_215";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0216() {
  using namespace knotwork;
  const std::string name = "generated_case_0216";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_216", {0.000f, 0.250f, 1.000f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_216", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_216", root, mat, trs({8.000f, 1.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_216", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_216", root, LightKind::Point, 17.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_216", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 23.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_216") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_216";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0217() {
  using namespace knotwork;
  const std::string name = "generated_case_0217";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_217", {0.500f, 0.500f, 0.000f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_217", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_217", root, mat, trs({9.000f, 0.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_217", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_217", root, LightKind::Point, 18.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_217", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 22.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 25.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_217") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({22.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_217";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 23.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0218() {
  using namespace knotwork;
  const std::string name = "generated_case_0218";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_218", {1.000f, 0.750f, 0.167f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_218", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_218", root, mat, trs({10.000f, 0.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_218", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_218", root, LightKind::Point, 19.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_218", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 24.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 27.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_218") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({24.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_218";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 25.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0219() {
  using namespace knotwork;
  const std::string name = "generated_case_0219";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_219", {0.000f, 1.000f, 0.333f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_219", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_219", root, mat, trs({11.000f, 0.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_219", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_219", root, LightKind::Point, 20.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_219", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 26.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 29.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_219") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({26.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_219";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 27.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0220() {
  using namespace knotwork;
  const std::string name = "generated_case_0220";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_220", {0.500f, 0.000f, 0.500f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_220", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_220", root, mat, trs({1.000f, 0.750f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_220", root, 55.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_220", root, LightKind::Point, 1.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_220", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 20.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_220") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_220";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0221() {
  using namespace knotwork;
  const std::string name = "generated_case_0221";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_221", {1.000f, 0.250f, 0.667f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_221", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_221", root, mat, trs({2.000f, 1.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_221", root, 56.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_221", root, LightKind::Point, 2.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_221", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 2.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_221") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({2.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_221";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 3.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0222() {
  using namespace knotwork;
  const std::string name = "generated_case_0222";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_222", {0.000f, 0.500f, 0.833f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_222", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_222", root, mat, trs({3.000f, 1.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_222", root, 57.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_222", root, LightKind::Point, 3.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_222", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 4.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_222") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({4.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_222";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 5.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0223() {
  using namespace knotwork;
  const std::string name = "generated_case_0223";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_223", {0.500f, 0.750f, 1.000f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_223", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_223", root, mat, trs({4.000f, 1.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_223", root, 58.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_223", root, LightKind::Point, 4.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_223", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 6.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_223") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({6.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_223";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 7.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0224() {
  using namespace knotwork;
  const std::string name = "generated_case_0224";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_224", {1.000f, 1.000f, 0.000f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_224", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_224", root, mat, trs({5.000f, 0.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_224", root, 59.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_224", root, LightKind::Point, 5.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_224", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_224") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_224";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0225() {
  using namespace knotwork;
  const std::string name = "generated_case_0225";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_225", {0.000f, 0.000f, 0.167f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_225", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_225", root, mat, trs({6.000f, 0.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_225", root, 60.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_225", root, LightKind::Point, 6.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_225", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_225") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_225";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0226() {
  using namespace knotwork;
  const std::string name = "generated_case_0226";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_226", {0.500f, 0.250f, 0.333f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_226", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_226", root, mat, trs({7.000f, 0.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_226", root, 61.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_226", root, LightKind::Point, 7.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_226", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 11.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_226") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_226";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0227() {
  using namespace knotwork;
  const std::string name = "generated_case_0227";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_227", {1.000f, 0.500f, 0.500f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_227", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_227", root, mat, trs({8.000f, 0.750f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_227", root, 62.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_227", root, LightKind::Point, 8.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_227", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 13.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_227") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_227";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0228() {
  using namespace knotwork;
  const std::string name = "generated_case_0228";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_228", {0.000f, 0.750f, 0.667f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_228", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_228", root, mat, trs({9.000f, 1.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_228", root, 63.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_228", root, LightKind::Point, 9.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_228", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 15.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_228") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_228";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0229() {
  using namespace knotwork;
  const std::string name = "generated_case_0229";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_229", {0.500f, 1.000f, 0.833f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_229", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_229", root, mat, trs({10.000f, 1.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_229", root, 64.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_229", root, LightKind::Point, 10.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_229", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 17.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_229") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_229";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0230() {
  using namespace knotwork;
  const std::string name = "generated_case_0230";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_230", {1.000f, 0.000f, 1.000f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_230", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_230", root, mat, trs({11.000f, 1.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_230", root, 65.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_230", root, LightKind::Point, 11.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_230", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 19.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_230") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_230";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0231() {
  using namespace knotwork;
  const std::string name = "generated_case_0231";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_231", {0.000f, 0.250f, 0.000f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_231", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_231", root, mat, trs({1.000f, 0.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_231", root, 66.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_231", root, LightKind::Point, 12.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_231", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 10.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_231") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_231";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0232() {
  using namespace knotwork;
  const std::string name = "generated_case_0232";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_232", {0.500f, 0.500f, 0.167f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_232", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_232", root, mat, trs({2.000f, 0.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_232", root, 67.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_232", root, LightKind::Point, 13.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_232", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 12.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_232") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_232";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0233() {
  using namespace knotwork;
  const std::string name = "generated_case_0233";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_233", {1.000f, 0.750f, 0.333f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_233", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_233", root, mat, trs({3.000f, 0.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_233", root, 68.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_233", root, LightKind::Point, 14.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_233", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 14.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_233") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_233";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0234() {
  using namespace knotwork;
  const std::string name = "generated_case_0234";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_234", {0.000f, 1.000f, 0.500f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_234", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_234", root, mat, trs({4.000f, 0.750f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_234", root, 69.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_234", root, LightKind::Point, 15.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_234", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 29.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_234") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_234";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0235() {
  using namespace knotwork;
  const std::string name = "generated_case_0235";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_235", {0.500f, 0.000f, 0.667f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_235", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_235", root, mat, trs({5.000f, 1.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_235", root, 70.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_235", root, LightKind::Point, 16.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_235", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 31.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_235") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_235";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0236() {
  using namespace knotwork;
  const std::string name = "generated_case_0236";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_236", {1.000f, 0.250f, 0.833f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_236", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_236", root, mat, trs({6.000f, 1.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_236", root, 71.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_236", root, LightKind::Point, 17.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_236", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 33.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_236") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_236";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0237() {
  using namespace knotwork;
  const std::string name = "generated_case_0237";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_237", {0.000f, 0.500f, 1.000f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_237", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_237", root, mat, trs({7.000f, 1.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_237", root, 72.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_237", root, LightKind::Point, 18.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_237", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 23.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 35.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_237") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({23.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_237";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 24.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0238() {
  using namespace knotwork;
  const std::string name = "generated_case_0238";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_238", {0.500f, 0.750f, 0.000f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_238", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_238", root, mat, trs({8.000f, 0.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_238", root, 73.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_238", root, LightKind::Point, 19.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_238", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_238") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_238";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0239() {
  using namespace knotwork;
  const std::string name = "generated_case_0239";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_239", {1.000f, 1.000f, 0.167f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_239", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_239", root, mat, trs({9.000f, 0.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_239", root, 74.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_239", root, LightKind::Point, 20.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_239", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_239") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_239";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0240() {
  using namespace knotwork;
  const std::string name = "generated_case_0240";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_240", {0.000f, 0.000f, 0.333f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_240", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_240", root, mat, trs({10.000f, 0.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_240", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_240", root, LightKind::Point, 1.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_240", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_240") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_240";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0241() {
  using namespace knotwork;
  const std::string name = "generated_case_0241";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_241", {0.500f, 0.250f, 0.500f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_241", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_241", root, mat, trs({11.000f, 0.750f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_241", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_241", root, LightKind::Point, 2.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_241", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_241") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_241";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0242() {
  using namespace knotwork;
  const std::string name = "generated_case_0242";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_242", {1.000f, 0.500f, 0.667f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_242", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_242", root, mat, trs({1.000f, 1.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_242", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_242", root, LightKind::Point, 3.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_242", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 5.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 0.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_242") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({5.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_242";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 6.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0243() {
  using namespace knotwork;
  const std::string name = "generated_case_0243";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_243", {0.000f, 0.750f, 0.833f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_243", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_243", root, mat, trs({2.000f, 1.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_243", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_243", root, LightKind::Point, 4.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_243", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 2.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_243") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_243";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0244() {
  using namespace knotwork;
  const std::string name = "generated_case_0244";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_244", {0.500f, 1.000f, 1.000f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_244", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_244", root, mat, trs({3.000f, 1.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_244", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_244", root, LightKind::Point, 5.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_244", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_244") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_244";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0245() {
  using namespace knotwork;
  const std::string name = "generated_case_0245";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_245", {1.000f, 0.000f, 0.000f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_245", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_245", root, mat, trs({4.000f, 0.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_245", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_245", root, LightKind::Point, 6.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_245", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 6.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_245") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_245";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0246() {
  using namespace knotwork;
  const std::string name = "generated_case_0246";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_246", {0.000f, 0.250f, 0.167f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_246", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_246", root, mat, trs({5.000f, 0.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_246", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_246", root, LightKind::Point, 7.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_246", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_246") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_246";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0247() {
  using namespace knotwork;
  const std::string name = "generated_case_0247";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_247", {0.500f, 0.500f, 0.333f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_247", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_247", root, mat, trs({6.000f, 0.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_247", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_247", root, LightKind::Point, 8.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_247", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 23.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_247") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_247";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0248() {
  using namespace knotwork;
  const std::string name = "generated_case_0248";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_248", {1.000f, 0.750f, 0.500f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_248", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_248", root, mat, trs({7.000f, 0.750f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_248", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_248", root, LightKind::Point, 9.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_248", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 25.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_248") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_248";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0249() {
  using namespace knotwork;
  const std::string name = "generated_case_0249";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_249", {0.000f, 1.000f, 0.667f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_249", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_249", root, mat, trs({8.000f, 1.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_249", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_249", root, LightKind::Point, 10.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_249", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 27.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_249") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_249";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0250() {
  using namespace knotwork;
  const std::string name = "generated_case_0250";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_250", {0.500f, 0.000f, 0.833f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_250", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_250", root, mat, trs({9.000f, 1.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_250", root, 55.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_250", root, LightKind::Point, 11.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_250", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 29.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_250") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_250";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0251() {
  using namespace knotwork;
  const std::string name = "generated_case_0251";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_251", {1.000f, 0.250f, 1.000f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_251", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_251", root, mat, trs({10.000f, 1.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_251", root, 56.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_251", root, LightKind::Point, 12.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_251", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 23.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 31.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_251") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({23.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_251";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 24.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0252() {
  using namespace knotwork;
  const std::string name = "generated_case_0252";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_252", {0.000f, 0.500f, 0.000f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_252", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_252", root, mat, trs({11.000f, 0.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_252", root, 57.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_252", root, LightKind::Point, 13.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_252", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 25.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 33.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_252") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({25.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_252";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 26.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0253() {
  using namespace knotwork;
  const std::string name = "generated_case_0253";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_253", {0.500f, 0.750f, 0.167f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_253", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_253", root, mat, trs({1.000f, 0.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_253", root, 58.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_253", root, LightKind::Point, 14.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_253", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 24.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_253") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_253";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0254() {
  using namespace knotwork;
  const std::string name = "generated_case_0254";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_254", {1.000f, 1.000f, 0.333f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_254", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_254", root, mat, trs({2.000f, 0.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_254", root, 59.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_254", root, LightKind::Point, 15.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_254", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 26.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_254") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_254";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0255() {
  using namespace knotwork;
  const std::string name = "generated_case_0255";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_255", {0.000f, 0.000f, 0.500f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_255", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_255", root, mat, trs({3.000f, 0.750f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_255", root, 60.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_255", root, LightKind::Point, 16.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_255", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 3.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -6.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_255") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({3.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_255";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 4.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0256() {
  using namespace knotwork;
  const std::string name = "generated_case_0256";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_256", {0.500f, 0.250f, 0.667f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_256", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_256", root, mat, trs({4.000f, 1.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_256", root, 61.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_256", root, LightKind::Point, 17.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_256", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 5.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_256") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({5.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_256";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 6.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0257() {
  using namespace knotwork;
  const std::string name = "generated_case_0257";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_257", {1.000f, 0.500f, 0.833f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_257", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_257", root, mat, trs({5.000f, 1.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_257", root, 62.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_257", root, LightKind::Point, 18.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_257", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -2.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_257") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_257";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0258() {
  using namespace knotwork;
  const std::string name = "generated_case_0258";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_258", {0.000f, 0.750f, 1.000f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_258", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_258", root, mat, trs({6.000f, 1.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_258", root, 63.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_258", root, LightKind::Point, 19.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_258", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 0.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_258") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_258";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0259() {
  using namespace knotwork;
  const std::string name = "generated_case_0259";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_259", {0.500f, 1.000f, 0.000f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_259", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_259", root, mat, trs({7.000f, 0.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_259", root, 64.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_259", root, LightKind::Point, 20.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_259", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 2.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_259") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_259";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0260() {
  using namespace knotwork;
  const std::string name = "generated_case_0260";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_260", {1.000f, 0.000f, 0.167f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_260", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_260", root, mat, trs({8.000f, 0.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_260", root, 65.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_260", root, LightKind::Point, 1.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_260", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 17.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_260") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_260";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0261() {
  using namespace knotwork;
  const std::string name = "generated_case_0261";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_261", {0.000f, 0.250f, 0.333f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_261", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_261", root, mat, trs({9.000f, 0.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_261", root, 66.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_261", root, LightKind::Point, 2.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_261", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 19.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_261") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_261";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0262() {
  using namespace knotwork;
  const std::string name = "generated_case_0262";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_262", {0.500f, 0.500f, 0.500f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_262", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_262", root, mat, trs({10.000f, 0.750f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_262", root, 67.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_262", root, LightKind::Point, 3.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_262", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 21.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_262") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_262";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0263() {
  using namespace knotwork;
  const std::string name = "generated_case_0263";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_263", {1.000f, 0.750f, 0.667f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_263", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_263", root, mat, trs({11.000f, 1.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_263", root, 68.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_263", root, LightKind::Point, 4.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_263", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 23.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_263") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_263";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0264() {
  using namespace knotwork;
  const std::string name = "generated_case_0264";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_264", {0.000f, 1.000f, 0.833f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_264", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_264", root, mat, trs({1.000f, 1.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_264", root, 69.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_264", root, LightKind::Point, 5.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_264", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 14.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_264") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_264";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0265() {
  using namespace knotwork;
  const std::string name = "generated_case_0265";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_265", {0.500f, 0.000f, 1.000f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_265", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_265", root, mat, trs({2.000f, 1.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_265", root, 70.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_265", root, LightKind::Point, 6.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_265", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 16.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_265") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_265";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0266() {
  using namespace knotwork;
  const std::string name = "generated_case_0266";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_266", {1.000f, 0.250f, 0.000f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_266", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_266", root, mat, trs({3.000f, 0.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_266", root, 71.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_266", root, LightKind::Point, 7.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_266", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 18.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_266") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_266";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0267() {
  using namespace knotwork;
  const std::string name = "generated_case_0267";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_267", {0.000f, 0.500f, 0.167f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_267", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_267", root, mat, trs({4.000f, 0.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_267", root, 72.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_267", root, LightKind::Point, 8.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_267", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 20.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_267") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_267";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0268() {
  using namespace knotwork;
  const std::string name = "generated_case_0268";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_268", {0.500f, 0.750f, 0.333f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_268", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_268", root, mat, trs({5.000f, 0.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_268", root, 73.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_268", root, LightKind::Point, 9.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_268", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 22.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_268") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_268";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0269() {
  using namespace knotwork;
  const std::string name = "generated_case_0269";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_269", {1.000f, 1.000f, 0.500f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_269", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_269", root, mat, trs({6.000f, 0.750f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_269", root, 74.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_269", root, LightKind::Point, 10.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_269", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 24.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_269") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_269";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0270() {
  using namespace knotwork;
  const std::string name = "generated_case_0270";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_270", {0.000f, 0.000f, 0.667f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_270", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_270", root, mat, trs({7.000f, 1.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_270", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_270", root, LightKind::Point, 11.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_270", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 22.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 26.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_270") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({22.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_270";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 23.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0271() {
  using namespace knotwork;
  const std::string name = "generated_case_0271";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_271", {0.500f, 0.250f, 0.833f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_271", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_271", root, mat, trs({8.000f, 1.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_271", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_271", root, LightKind::Point, 12.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_271", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 24.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 28.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_271") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({24.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_271";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 25.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0272() {
  using namespace knotwork;
  const std::string name = "generated_case_0272";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_272", {1.000f, 0.500f, 1.000f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_272", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_272", root, mat, trs({9.000f, 1.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_272", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_272", root, LightKind::Point, 13.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_272", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_272") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_272";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0273() {
  using namespace knotwork;
  const std::string name = "generated_case_0273";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_273", {0.000f, 0.750f, 0.000f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_273", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_273", root, mat, trs({10.000f, 0.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_273", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_273", root, LightKind::Point, 14.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_273", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 11.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_273") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_273";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0274() {
  using namespace knotwork;
  const std::string name = "generated_case_0274";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_274", {0.500f, 1.000f, 0.167f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_274", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_274", root, mat, trs({11.000f, 0.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_274", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_274", root, LightKind::Point, 15.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_274", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 13.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_274") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_274";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0275() {
  using namespace knotwork;
  const std::string name = "generated_case_0275";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_275", {1.000f, 0.000f, 0.333f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_275", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_275", root, mat, trs({1.000f, 0.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_275", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_275", root, LightKind::Point, 16.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_275", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 4.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_275") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({4.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_275";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 5.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0276() {
  using namespace knotwork;
  const std::string name = "generated_case_0276";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_276", {0.000f, 0.250f, 0.500f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_276", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_276", root, mat, trs({2.000f, 0.750f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_276", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_276", root, LightKind::Point, 17.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_276", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 6.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 6.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_276") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({6.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_276";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 7.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0277() {
  using namespace knotwork;
  const std::string name = "generated_case_0277";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_277", {0.500f, 0.500f, 0.667f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_277", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_277", root, mat, trs({3.000f, 1.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_277", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_277", root, LightKind::Point, 18.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_277", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_277") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_277";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0278() {
  using namespace knotwork;
  const std::string name = "generated_case_0278";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_278", {1.000f, 0.750f, 0.833f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_278", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_278", root, mat, trs({4.000f, 1.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_278", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_278", root, LightKind::Point, 19.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_278", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 10.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_278") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_278";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0279() {
  using namespace knotwork;
  const std::string name = "generated_case_0279";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_279", {0.000f, 1.000f, 1.000f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_279", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_279", root, mat, trs({5.000f, 1.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_279", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_279", root, LightKind::Point, 20.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_279", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 12.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_279") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_279";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0280() {
  using namespace knotwork;
  const std::string name = "generated_case_0280";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_280", {0.500f, 0.000f, 0.000f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_280", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_280", root, mat, trs({6.000f, 0.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_280", root, 55.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_280", root, LightKind::Point, 1.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_280", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 14.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_280") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_280";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0281() {
  using namespace knotwork;
  const std::string name = "generated_case_0281";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_281", {1.000f, 0.250f, 0.167f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_281", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_281", root, mat, trs({7.000f, 0.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_281", root, 56.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_281", root, LightKind::Point, 2.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_281", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 16.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_281") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_281";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0282() {
  using namespace knotwork;
  const std::string name = "generated_case_0282";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_282", {0.000f, 0.500f, 0.333f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_282", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_282", root, mat, trs({8.000f, 0.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_282", root, 57.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_282", root, LightKind::Point, 3.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_282", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 18.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_282") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_282";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0283() {
  using namespace knotwork;
  const std::string name = "generated_case_0283";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_283", {0.500f, 0.750f, 0.500f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_283", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_283", root, mat, trs({9.000f, 0.750f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_283", root, 58.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_283", root, LightKind::Point, 4.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_283", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 20.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_283") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_283";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0284() {
  using namespace knotwork;
  const std::string name = "generated_case_0284";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_284", {1.000f, 1.000f, 0.667f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_284", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_284", root, mat, trs({10.000f, 1.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_284", root, 59.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_284", root, LightKind::Point, 5.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_284", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 22.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 22.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_284") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({22.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_284";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 23.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0285() {
  using namespace knotwork;
  const std::string name = "generated_case_0285";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_285", {0.000f, 0.000f, 0.833f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_285", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_285", root, mat, trs({11.000f, 1.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_285", root, 60.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_285", root, LightKind::Point, 6.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_285", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 24.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 24.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_285") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({24.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_285";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 25.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0286() {
  using namespace knotwork;
  const std::string name = "generated_case_0286";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_286", {0.500f, 0.250f, 1.000f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_286", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_286", root, mat, trs({1.000f, 1.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_286", root, 61.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_286", root, LightKind::Point, 7.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_286", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 28.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_286") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_286";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0287() {
  using namespace knotwork;
  const std::string name = "generated_case_0287";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_287", {1.000f, 0.500f, 0.000f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_287", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_287", root, mat, trs({2.000f, 0.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_287", root, 62.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_287", root, LightKind::Point, 8.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_287", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 30.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_287") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_287";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0288() {
  using namespace knotwork;
  const std::string name = "generated_case_0288";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_288", {0.000f, 0.750f, 0.167f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_288", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_288", root, mat, trs({3.000f, 0.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_288", root, 63.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_288", root, LightKind::Point, 9.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_288", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 32.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_288") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_288";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0289() {
  using namespace knotwork;
  const std::string name = "generated_case_0289";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_289", {0.500f, 1.000f, 0.333f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_289", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_289", root, mat, trs({4.000f, 0.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_289", root, 64.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_289", root, LightKind::Point, 10.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_289", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 4.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 0.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_289") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({4.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_289";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 5.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0290() {
  using namespace knotwork;
  const std::string name = "generated_case_0290";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_290", {1.000f, 0.000f, 0.500f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_290", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_290", root, mat, trs({5.000f, 0.750f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_290", root, 65.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_290", root, LightKind::Point, 11.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_290", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 6.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 2.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_290") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({6.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_290";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 7.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0291() {
  using namespace knotwork;
  const std::string name = "generated_case_0291";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_291", {0.000f, 0.250f, 0.667f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_291", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_291", root, mat, trs({6.000f, 1.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_291", root, 66.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_291", root, LightKind::Point, 12.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_291", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_291") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_291";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0292() {
  using namespace knotwork;
  const std::string name = "generated_case_0292";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_292", {0.500f, 0.500f, 0.833f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_292", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_292", root, mat, trs({7.000f, 1.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_292", root, 67.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_292", root, LightKind::Point, 13.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_292", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 6.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_292") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_292";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0293() {
  using namespace knotwork;
  const std::string name = "generated_case_0293";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_293", {1.000f, 0.750f, 1.000f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_293", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_293", root, mat, trs({8.000f, 1.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_293", root, 68.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_293", root, LightKind::Point, 14.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_293", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_293") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_293";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0294() {
  using namespace knotwork;
  const std::string name = "generated_case_0294";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_294", {0.000f, 1.000f, 0.000f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_294", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_294", root, mat, trs({9.000f, 0.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_294", root, 69.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_294", root, LightKind::Point, 15.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_294", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 10.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_294") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_294";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0295() {
  using namespace knotwork;
  const std::string name = "generated_case_0295";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_295", {0.500f, 0.000f, 0.167f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_295", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_295", root, mat, trs({10.000f, 0.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_295", root, 70.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_295", root, LightKind::Point, 16.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_295", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 12.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_295") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_295";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0296() {
  using namespace knotwork;
  const std::string name = "generated_case_0296";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_296", {1.000f, 0.250f, 0.333f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_296", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_296", root, mat, trs({11.000f, 0.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_296", root, 71.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_296", root, LightKind::Point, 17.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_296", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 14.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_296") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_296";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0297() {
  using namespace knotwork;
  const std::string name = "generated_case_0297";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_297", {0.000f, 0.500f, 0.500f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_297", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_297", root, mat, trs({1.000f, 0.750f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_297", root, 72.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_297", root, LightKind::Point, 18.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_297", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_297") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_297";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0298() {
  using namespace knotwork;
  const std::string name = "generated_case_0298";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_298", {0.500f, 0.750f, 0.667f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_298", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_298", root, mat, trs({2.000f, 1.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_298", root, 73.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_298", root, LightKind::Point, 19.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_298", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_298") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_298";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0299() {
  using namespace knotwork;
  const std::string name = "generated_case_0299";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_299", {1.000f, 1.000f, 0.833f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_299", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_299", root, mat, trs({3.000f, 1.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_299", root, 74.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_299", root, LightKind::Point, 20.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_299", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 22.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_299") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_299";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0300() {
  using namespace knotwork;
  const std::string name = "generated_case_0300";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_300", {0.000f, 0.000f, 1.000f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_300", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_300", root, mat, trs({4.000f, 1.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_300", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_300", root, LightKind::Point, 1.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_300", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 24.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_300") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_300";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0301() {
  using namespace knotwork;
  const std::string name = "generated_case_0301";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_301", {0.500f, 0.250f, 0.000f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_301", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_301", root, mat, trs({5.000f, 0.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_301", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_301", root, LightKind::Point, 2.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_301", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 26.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_301") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_301";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0302() {
  using namespace knotwork;
  const std::string name = "generated_case_0302";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_302", {1.000f, 0.500f, 0.167f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_302", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_302", root, mat, trs({6.000f, 0.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_302", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_302", root, LightKind::Point, 3.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_302", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 28.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_302") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_302";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0303() {
  using namespace knotwork;
  const std::string name = "generated_case_0303";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_303", {0.000f, 0.750f, 0.333f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_303", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_303", root, mat, trs({7.000f, 0.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_303", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_303", root, LightKind::Point, 4.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_303", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 30.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_303") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_303";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0304() {
  using namespace knotwork;
  const std::string name = "generated_case_0304";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_304", {0.500f, 1.000f, 0.500f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_304", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_304", root, mat, trs({8.000f, 0.750f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_304", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_304", root, LightKind::Point, 5.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_304", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 23.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 32.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_304") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({23.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_304";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 24.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0305() {
  using namespace knotwork;
  const std::string name = "generated_case_0305";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_305", {1.000f, 0.000f, 0.667f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_305", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_305", root, mat, trs({9.000f, 1.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_305", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_305", root, LightKind::Point, 6.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_305", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 25.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 34.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_305") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({25.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_305";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 26.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0306() {
  using namespace knotwork;
  const std::string name = "generated_case_0306";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_306", {0.000f, 0.250f, 0.833f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_306", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_306", root, mat, trs({10.000f, 1.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_306", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_306", root, LightKind::Point, 7.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_306", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 2.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_306") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_306";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0307() {
  using namespace knotwork;
  const std::string name = "generated_case_0307";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_307", {0.500f, 0.500f, 1.000f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_307", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_307", root, mat, trs({11.000f, 1.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_307", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_307", root, LightKind::Point, 8.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_307", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_307") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_307";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0308() {
  using namespace knotwork;
  const std::string name = "generated_case_0308";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_308", {1.000f, 0.750f, 0.000f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_308", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_308", root, mat, trs({1.000f, 0.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_308", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_308", root, LightKind::Point, 9.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_308", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 3.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_308") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({3.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_308";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 4.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0309() {
  using namespace knotwork;
  const std::string name = "generated_case_0309";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_309", {0.000f, 1.000f, 0.167f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_309", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_309", root, mat, trs({2.000f, 0.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_309", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_309", root, LightKind::Point, 10.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_309", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 5.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_309") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({5.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_309";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 6.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0310() {
  using namespace knotwork;
  const std::string name = "generated_case_0310";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_310", {0.500f, 0.000f, 0.333f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_310", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_310", root, mat, trs({3.000f, 0.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_310", root, 55.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_310", root, LightKind::Point, 11.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_310", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_310") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_310";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0311() {
  using namespace knotwork;
  const std::string name = "generated_case_0311";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_311", {1.000f, 0.250f, 0.500f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_311", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_311", root, mat, trs({4.000f, 0.750f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_311", root, 56.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_311", root, LightKind::Point, 12.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_311", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_311") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_311";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0312() {
  using namespace knotwork;
  const std::string name = "generated_case_0312";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_312", {0.000f, 0.500f, 0.667f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_312", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_312", root, mat, trs({5.000f, 1.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_312", root, 57.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_312", root, LightKind::Point, 13.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_312", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 16.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_312") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_312";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0313() {
  using namespace knotwork;
  const std::string name = "generated_case_0313";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_313", {0.500f, 0.750f, 0.833f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_313", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_313", root, mat, trs({6.000f, 1.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_313", root, 58.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_313", root, LightKind::Point, 14.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_313", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 18.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_313") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_313";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0314() {
  using namespace knotwork;
  const std::string name = "generated_case_0314";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_314", {1.000f, 1.000f, 1.000f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_314", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_314", root, mat, trs({7.000f, 1.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_314", root, 59.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_314", root, LightKind::Point, 15.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_314", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 20.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_314") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_314";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0315() {
  using namespace knotwork;
  const std::string name = "generated_case_0315";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_315", {0.000f, 0.000f, 0.000f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_315", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_315", root, mat, trs({8.000f, 0.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_315", root, 60.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_315", root, LightKind::Point, 16.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_315", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 22.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_315") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_315";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0316() {
  using namespace knotwork;
  const std::string name = "generated_case_0316";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_316", {0.500f, 0.250f, 0.167f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_316", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_316", root, mat, trs({9.000f, 0.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_316", root, 61.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_316", root, LightKind::Point, 17.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_316", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 24.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_316") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_316";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0317() {
  using namespace knotwork;
  const std::string name = "generated_case_0317";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_317", {1.000f, 0.500f, 0.333f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_317", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_317", root, mat, trs({10.000f, 0.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_317", root, 62.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_317", root, LightKind::Point, 18.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_317", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 26.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_317") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_317";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0318() {
  using namespace knotwork;
  const std::string name = "generated_case_0318";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_318", {0.000f, 0.750f, 0.500f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_318", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_318", root, mat, trs({11.000f, 0.750f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_318", root, 63.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_318", root, LightKind::Point, 19.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_318", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 23.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 28.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_318") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({23.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_318";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 24.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0319() {
  using namespace knotwork;
  const std::string name = "generated_case_0319";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_319", {0.500f, 1.000f, 0.667f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_319", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_319", root, mat, trs({1.000f, 1.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_319", root, 64.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_319", root, LightKind::Point, 20.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_319", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 19.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_319") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_319";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0320() {
  using namespace knotwork;
  const std::string name = "generated_case_0320";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_320", {1.000f, 0.000f, 0.833f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_320", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_320", root, mat, trs({2.000f, 1.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_320", root, 65.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_320", root, LightKind::Point, 1.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_320", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 21.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_320") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_320";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0321() {
  using namespace knotwork;
  const std::string name = "generated_case_0321";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_321", {0.000f, 0.250f, 1.000f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_321", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_321", root, mat, trs({3.000f, 1.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_321", root, 66.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_321", root, LightKind::Point, 2.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_321", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 23.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_321") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_321";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0322() {
  using namespace knotwork;
  const std::string name = "generated_case_0322";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_322", {0.500f, 0.500f, 0.000f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_322", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_322", root, mat, trs({4.000f, 0.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_322", root, 67.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_322", root, LightKind::Point, 3.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_322", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 25.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_322") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_322";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0323() {
  using namespace knotwork;
  const std::string name = "generated_case_0323";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_323", {1.000f, 0.750f, 0.167f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_323", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_323", root, mat, trs({5.000f, 0.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_323", root, 68.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_323", root, LightKind::Point, 4.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_323", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 5.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_323") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({5.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_323";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 6.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0324() {
  using namespace knotwork;
  const std::string name = "generated_case_0324";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_324", {0.000f, 1.000f, 0.333f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_324", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_324", root, mat, trs({6.000f, 0.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_324", root, 69.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_324", root, LightKind::Point, 5.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_324", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_324") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_324";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0325() {
  using namespace knotwork;
  const std::string name = "generated_case_0325";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_325", {0.500f, 0.000f, 0.500f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_325", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_325", root, mat, trs({7.000f, 0.750f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_325", root, 70.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_325", root, LightKind::Point, 6.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_325", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 10.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_325") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_325";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0326() {
  using namespace knotwork;
  const std::string name = "generated_case_0326";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_326", {1.000f, 0.250f, 0.667f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_326", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_326", root, mat, trs({8.000f, 1.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_326", root, 71.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_326", root, LightKind::Point, 7.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_326", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 12.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_326") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_326";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0327() {
  using namespace knotwork;
  const std::string name = "generated_case_0327";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_327", {0.000f, 0.500f, 0.833f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_327", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_327", root, mat, trs({9.000f, 1.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_327", root, 72.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_327", root, LightKind::Point, 8.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_327", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 14.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_327") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_327";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0328() {
  using namespace knotwork;
  const std::string name = "generated_case_0328";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_328", {0.500f, 0.750f, 1.000f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_328", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_328", root, mat, trs({10.000f, 1.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_328", root, 73.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_328", root, LightKind::Point, 9.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_328", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 16.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_328") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_328";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0329() {
  using namespace knotwork;
  const std::string name = "generated_case_0329";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_329", {1.000f, 1.000f, 0.000f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_329", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_329", root, mat, trs({11.000f, 0.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_329", root, 74.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_329", root, LightKind::Point, 10.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_329", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 18.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_329") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_329";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0330() {
  using namespace knotwork;
  const std::string name = "generated_case_0330";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_330", {0.000f, 0.000f, 0.167f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_330", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_330", root, mat, trs({1.000f, 0.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_330", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_330", root, LightKind::Point, 11.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_330", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_330") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_330";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0331() {
  using namespace knotwork;
  const std::string name = "generated_case_0331";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_331", {0.500f, 0.250f, 0.333f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_331", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_331", root, mat, trs({2.000f, 0.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_331", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_331", root, LightKind::Point, 12.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_331", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 11.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_331") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_331";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0332() {
  using namespace knotwork;
  const std::string name = "generated_case_0332";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_332", {1.000f, 0.500f, 0.500f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_332", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_332", root, mat, trs({3.000f, 0.750f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_332", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_332", root, LightKind::Point, 13.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_332", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 13.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_332") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_332";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0333() {
  using namespace knotwork;
  const std::string name = "generated_case_0333";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_333", {0.000f, 0.750f, 0.667f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_333", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_333", root, mat, trs({4.000f, 1.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_333", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_333", root, LightKind::Point, 14.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_333", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 15.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_333") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_333";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0334() {
  using namespace knotwork;
  const std::string name = "generated_case_0334";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_334", {0.500f, 1.000f, 0.833f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_334", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_334", root, mat, trs({5.000f, 1.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_334", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_334", root, LightKind::Point, 15.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_334", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 17.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_334") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_334";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0335() {
  using namespace knotwork;
  const std::string name = "generated_case_0335";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_335", {1.000f, 0.000f, 1.000f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_335", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_335", root, mat, trs({6.000f, 1.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_335", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_335", root, LightKind::Point, 16.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_335", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 19.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_335") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_335";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0336() {
  using namespace knotwork;
  const std::string name = "generated_case_0336";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_336", {0.000f, 0.250f, 0.000f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_336", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_336", root, mat, trs({7.000f, 0.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_336", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_336", root, LightKind::Point, 17.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_336", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 21.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_336") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_336";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0337() {
  using namespace knotwork;
  const std::string name = "generated_case_0337";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_337", {0.500f, 0.500f, 0.167f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_337", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_337", root, mat, trs({8.000f, 0.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_337", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_337", root, LightKind::Point, 18.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_337", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 22.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 23.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_337") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({22.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_337";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 23.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0338() {
  using namespace knotwork;
  const std::string name = "generated_case_0338";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_338", {1.000f, 0.750f, 0.333f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_338", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_338", root, mat, trs({9.000f, 0.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_338", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_338", root, LightKind::Point, 19.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_338", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 24.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 38.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_338") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({24.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_338";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 25.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0339() {
  using namespace knotwork;
  const std::string name = "generated_case_0339";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_339", {0.000f, 1.000f, 0.500f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_339", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_339", root, mat, trs({10.000f, 0.750f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_339", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_339", root, LightKind::Point, 20.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_339", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 26.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 40.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_339") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({26.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_339";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 27.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0340() {
  using namespace knotwork;
  const std::string name = "generated_case_0340";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_340", {0.500f, 0.000f, 0.667f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_340", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_340", root, mat, trs({11.000f, 1.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_340", root, 55.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_340", root, LightKind::Point, 1.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_340", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_340") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_340";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0341() {
  using namespace knotwork;
  const std::string name = "generated_case_0341";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_341", {1.000f, 0.250f, 0.833f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_341", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_341", root, mat, trs({1.000f, 1.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_341", root, 56.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_341", root, LightKind::Point, 2.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_341", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 2.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_341") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({2.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_341";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 3.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0342() {
  using namespace knotwork;
  const std::string name = "generated_case_0342";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_342", {0.000f, 0.500f, 1.000f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_342", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_342", root, mat, trs({2.000f, 1.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_342", root, 57.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_342", root, LightKind::Point, 3.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_342", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 4.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_342") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({4.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_342";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 5.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0343() {
  using namespace knotwork;
  const std::string name = "generated_case_0343";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_343", {0.500f, 0.750f, 0.000f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_343", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_343", root, mat, trs({3.000f, 0.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_343", root, 58.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_343", root, LightKind::Point, 4.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_343", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 6.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_343") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({6.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_343";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 7.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0344() {
  using namespace knotwork;
  const std::string name = "generated_case_0344";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_344", {1.000f, 1.000f, 0.167f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_344", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_344", root, mat, trs({4.000f, 0.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_344", root, 59.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_344", root, LightKind::Point, 5.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_344", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_344") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_344";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0345() {
  using namespace knotwork;
  const std::string name = "generated_case_0345";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_345", {0.000f, 0.000f, 0.333f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_345", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_345", root, mat, trs({5.000f, 0.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_345", root, 60.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_345", root, LightKind::Point, 6.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_345", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_345") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_345";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0346() {
  using namespace knotwork;
  const std::string name = "generated_case_0346";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_346", {0.500f, 0.250f, 0.500f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_346", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_346", root, mat, trs({6.000f, 0.750f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_346", root, 61.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_346", root, LightKind::Point, 7.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_346", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_346") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_346";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0347() {
  using namespace knotwork;
  const std::string name = "generated_case_0347";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_347", {1.000f, 0.500f, 0.667f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_347", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_347", root, mat, trs({7.000f, 1.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_347", root, 62.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_347", root, LightKind::Point, 8.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_347", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 11.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_347") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_347";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0348() {
  using namespace knotwork;
  const std::string name = "generated_case_0348";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_348", {0.000f, 0.750f, 0.833f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_348", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_348", root, mat, trs({8.000f, 1.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_348", root, 63.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_348", root, LightKind::Point, 9.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_348", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 13.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_348") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_348";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0349() {
  using namespace knotwork;
  const std::string name = "generated_case_0349";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_349", {0.500f, 1.000f, 1.000f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_349", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_349", root, mat, trs({9.000f, 1.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_349", root, 64.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_349", root, LightKind::Point, 10.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_349", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 15.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_349") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_349";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0350() {
  using namespace knotwork;
  const std::string name = "generated_case_0350";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_350", {1.000f, 0.000f, 0.000f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_350", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_350", root, mat, trs({10.000f, 0.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_350", root, 65.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_350", root, LightKind::Point, 11.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_350", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 17.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_350") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_350";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0351() {
  using namespace knotwork;
  const std::string name = "generated_case_0351";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_351", {0.000f, 0.250f, 0.167f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_351", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_351", root, mat, trs({11.000f, 0.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_351", root, 66.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_351", root, LightKind::Point, 12.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_351", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 22.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 32.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_351") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({22.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_351";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 23.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0352() {
  using namespace knotwork;
  const std::string name = "generated_case_0352";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_352", {0.500f, 0.500f, 0.333f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_352", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_352", root, mat, trs({1.000f, 0.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_352", root, 67.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_352", root, LightKind::Point, 13.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_352", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 23.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_352") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_352";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0353() {
  using namespace knotwork;
  const std::string name = "generated_case_0353";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_353", {1.000f, 0.750f, 0.500f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_353", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_353", root, mat, trs({2.000f, 0.750f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_353", root, 68.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_353", root, LightKind::Point, 14.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_353", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 25.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_353") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_353";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0354() {
  using namespace knotwork;
  const std::string name = "generated_case_0354";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_354", {0.000f, 1.000f, 0.667f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_354", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_354", root, mat, trs({3.000f, 1.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_354", root, 69.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_354", root, LightKind::Point, 15.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_354", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 27.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_354") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_354";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0355() {
  using namespace knotwork;
  const std::string name = "generated_case_0355";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_355", {0.500f, 0.000f, 0.833f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_355", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_355", root, mat, trs({4.000f, 1.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_355", root, 70.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_355", root, LightKind::Point, 16.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_355", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 29.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_355") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_355";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0356() {
  using namespace knotwork;
  const std::string name = "generated_case_0356";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_356", {1.000f, 0.250f, 1.000f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_356", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_356", root, mat, trs({5.000f, 1.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_356", root, 71.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_356", root, LightKind::Point, 17.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_356", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 31.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_356") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_356";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0357() {
  using namespace knotwork;
  const std::string name = "generated_case_0357";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_357", {0.000f, 0.500f, 0.000f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_357", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_357", root, mat, trs({6.000f, 0.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_357", root, 72.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_357", root, LightKind::Point, 18.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_357", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 6.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_357") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({6.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_357";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 7.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0358() {
  using namespace knotwork;
  const std::string name = "generated_case_0358";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_358", {0.500f, 0.750f, 0.167f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_358", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_358", root, mat, trs({7.000f, 0.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_358", root, 73.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_358", root, LightKind::Point, 19.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_358", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_358") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_358";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0359() {
  using namespace knotwork;
  const std::string name = "generated_case_0359";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_359", {1.000f, 1.000f, 0.333f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_359", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_359", root, mat, trs({8.000f, 0.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_359", root, 74.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_359", root, LightKind::Point, 20.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_359", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_359") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_359";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0360() {
  using namespace knotwork;
  const std::string name = "generated_case_0360";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_360", {0.000f, 0.000f, 0.500f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_360", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_360", root, mat, trs({9.000f, 0.750f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_360", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_360", root, LightKind::Point, 1.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_360", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_360") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_360";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0361() {
  using namespace knotwork;
  const std::string name = "generated_case_0361";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_361", {0.500f, 0.250f, 0.667f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_361", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_361", root, mat, trs({10.000f, 1.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_361", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_361", root, LightKind::Point, 2.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_361", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_361") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_361";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0362() {
  using namespace knotwork;
  const std::string name = "generated_case_0362";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_362", {1.000f, 0.500f, 0.833f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_362", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_362", root, mat, trs({11.000f, 1.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_362", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_362", root, LightKind::Point, 3.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_362", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_362") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_362";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0363() {
  using namespace knotwork;
  const std::string name = "generated_case_0363";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_363", {0.000f, 0.750f, 1.000f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_363", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_363", root, mat, trs({1.000f, 1.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_363", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_363", root, LightKind::Point, 4.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_363", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 0.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_363") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_363";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0364() {
  using namespace knotwork;
  const std::string name = "generated_case_0364";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_364", {0.500f, 1.000f, 0.000f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_364", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_364", root, mat, trs({2.000f, 0.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_364", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_364", root, LightKind::Point, 5.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_364", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 15.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_364") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_364";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0365() {
  using namespace knotwork;
  const std::string name = "generated_case_0365";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_365", {1.000f, 0.000f, 0.167f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_365", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_365", root, mat, trs({3.000f, 0.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_365", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_365", root, LightKind::Point, 6.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_365", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 17.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_365") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_365";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0366() {
  using namespace knotwork;
  const std::string name = "generated_case_0366";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_366", {0.000f, 0.250f, 0.333f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_366", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_366", root, mat, trs({4.000f, 0.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_366", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_366", root, LightKind::Point, 7.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_366", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 19.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_366") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_366";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0367() {
  using namespace knotwork;
  const std::string name = "generated_case_0367";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_367", {0.500f, 0.500f, 0.500f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_367", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_367", root, mat, trs({5.000f, 0.750f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_367", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_367", root, LightKind::Point, 8.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_367", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 21.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_367") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_367";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0368() {
  using namespace knotwork;
  const std::string name = "generated_case_0368";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_368", {1.000f, 0.750f, 0.667f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_368", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_368", root, mat, trs({6.000f, 1.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_368", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_368", root, LightKind::Point, 9.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_368", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 23.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_368") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_368";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0369() {
  using namespace knotwork;
  const std::string name = "generated_case_0369";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_369", {0.000f, 1.000f, 0.833f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_369", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_369", root, mat, trs({7.000f, 1.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_369", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_369", root, LightKind::Point, 10.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_369", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 25.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_369") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_369";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0370() {
  using namespace knotwork;
  const std::string name = "generated_case_0370";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_370", {0.500f, 0.000f, 1.000f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_370", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_370", root, mat, trs({8.000f, 1.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_370", root, 55.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_370", root, LightKind::Point, 11.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_370", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 27.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_370") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_370";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0371() {
  using namespace knotwork;
  const std::string name = "generated_case_0371";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_371", {1.000f, 0.250f, 0.000f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_371", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_371", root, mat, trs({9.000f, 0.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_371", root, 56.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_371", root, LightKind::Point, 12.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_371", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 23.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 29.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_371") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({23.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_371";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 24.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0372() {
  using namespace knotwork;
  const std::string name = "generated_case_0372";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_372", {0.000f, 0.500f, 0.167f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_372", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_372", root, mat, trs({10.000f, 0.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_372", root, 57.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_372", root, LightKind::Point, 13.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_372", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 25.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 31.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_372") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({25.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_372";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 26.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0373() {
  using namespace knotwork;
  const std::string name = "generated_case_0373";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_373", {0.500f, 0.750f, 0.333f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_373", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_373", root, mat, trs({11.000f, 0.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_373", root, 58.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_373", root, LightKind::Point, 14.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_373", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 27.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 33.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_373") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({27.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_373";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 28.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0374() {
  using namespace knotwork;
  const std::string name = "generated_case_0374";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_374", {1.000f, 1.000f, 0.500f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_374", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_374", root, mat, trs({1.000f, 0.750f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_374", root, 59.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_374", root, LightKind::Point, 15.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_374", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 1.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -10.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_374") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({1.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_374";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 2.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0375() {
  using namespace knotwork;
  const std::string name = "generated_case_0375";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_375", {0.000f, 0.000f, 0.667f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_375", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_375", root, mat, trs({2.000f, 1.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_375", root, 60.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_375", root, LightKind::Point, 16.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_375", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 3.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_375") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({3.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_375";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 4.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0376() {
  using namespace knotwork;
  const std::string name = "generated_case_0376";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_376", {0.500f, 0.250f, 0.833f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_376", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_376", root, mat, trs({3.000f, 1.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_376", root, 61.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_376", root, LightKind::Point, 17.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_376", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 5.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -6.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_376") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({5.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_376";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 6.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0377() {
  using namespace knotwork;
  const std::string name = "generated_case_0377";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_377", {1.000f, 0.500f, 1.000f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_377", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_377", root, mat, trs({4.000f, 1.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_377", root, 62.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_377", root, LightKind::Point, 18.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_377", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_377") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_377";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0378() {
  using namespace knotwork;
  const std::string name = "generated_case_0378";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_378", {0.000f, 0.750f, 0.000f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_378", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_378", root, mat, trs({5.000f, 0.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_378", root, 63.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_378", root, LightKind::Point, 19.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_378", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 11.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_378") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_378";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0379() {
  using namespace knotwork;
  const std::string name = "generated_case_0379";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_379", {0.500f, 1.000f, 0.167f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_379", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_379", root, mat, trs({6.000f, 0.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_379", root, 64.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_379", root, LightKind::Point, 20.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_379", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 13.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_379") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_379";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0380() {
  using namespace knotwork;
  const std::string name = "generated_case_0380";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_380", {1.000f, 0.000f, 0.333f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_380", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_380", root, mat, trs({7.000f, 0.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_380", root, 65.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_380", root, LightKind::Point, 1.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_380", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 15.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_380") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_380";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0381() {
  using namespace knotwork;
  const std::string name = "generated_case_0381";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_381", {0.000f, 0.250f, 0.500f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_381", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_381", root, mat, trs({8.000f, 0.750f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_381", root, 66.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_381", root, LightKind::Point, 2.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_381", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 17.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_381") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_381";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0382() {
  using namespace knotwork;
  const std::string name = "generated_case_0382";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_382", {0.500f, 0.500f, 0.667f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_382", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_382", root, mat, trs({9.000f, 1.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_382", root, 67.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_382", root, LightKind::Point, 3.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_382", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 19.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_382") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_382";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0383() {
  using namespace knotwork;
  const std::string name = "generated_case_0383";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_383", {1.000f, 0.750f, 0.833f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_383", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_383", root, mat, trs({10.000f, 1.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_383", root, 68.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_383", root, LightKind::Point, 4.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_383", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 21.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_383") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_383";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0384() {
  using namespace knotwork;
  const std::string name = "generated_case_0384";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_384", {0.000f, 1.000f, 1.000f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_384", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_384", root, mat, trs({11.000f, 1.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_384", root, 69.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_384", root, LightKind::Point, 5.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_384", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 23.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_384") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_384";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0385() {
  using namespace knotwork;
  const std::string name = "generated_case_0385";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_385", {0.500f, 0.000f, 0.000f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_385", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_385", root, mat, trs({1.000f, 0.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_385", root, 70.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_385", root, LightKind::Point, 6.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_385", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 14.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_385") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_385";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0386() {
  using namespace knotwork;
  const std::string name = "generated_case_0386";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_386", {1.000f, 0.250f, 0.167f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_386", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_386", root, mat, trs({2.000f, 0.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_386", root, 71.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_386", root, LightKind::Point, 7.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_386", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 16.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_386") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_386";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0387() {
  using namespace knotwork;
  const std::string name = "generated_case_0387";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_387", {0.000f, 0.500f, 0.333f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_387", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_387", root, mat, trs({3.000f, 0.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_387", root, 72.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_387", root, LightKind::Point, 8.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_387", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 18.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_387") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_387";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0388() {
  using namespace knotwork;
  const std::string name = "generated_case_0388";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_388", {0.500f, 0.750f, 0.500f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_388", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_388", root, mat, trs({4.000f, 0.750f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_388", root, 73.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_388", root, LightKind::Point, 9.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_388", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 20.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_388") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_388";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0389() {
  using namespace knotwork;
  const std::string name = "generated_case_0389";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_389", {1.000f, 1.000f, 0.667f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_389", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_389", root, mat, trs({5.000f, 1.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_389", root, 74.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_389", root, LightKind::Point, 10.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_389", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 22.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_389") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_389";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0390() {
  using namespace knotwork;
  const std::string name = "generated_case_0390";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_390", {0.000f, 0.000f, 0.833f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_390", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_390", root, mat, trs({6.000f, 1.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_390", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_390", root, LightKind::Point, 11.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_390", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 22.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 37.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_390") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({22.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_390";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 23.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0391() {
  using namespace knotwork;
  const std::string name = "generated_case_0391";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_391", {0.500f, 0.250f, 1.000f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_391", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_391", root, mat, trs({7.000f, 1.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_391", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_391", root, LightKind::Point, 12.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_391", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_391") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_391";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0392() {
  using namespace knotwork;
  const std::string name = "generated_case_0392";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_392", {1.000f, 0.500f, 0.000f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_392", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_392", root, mat, trs({8.000f, 0.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_392", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_392", root, LightKind::Point, 13.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_392", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_392") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_392";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0393() {
  using namespace knotwork;
  const std::string name = "generated_case_0393";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_393", {0.000f, 0.750f, 0.167f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_393", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_393", root, mat, trs({9.000f, 0.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_393", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_393", root, LightKind::Point, 14.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_393", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_393") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_393";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0394() {
  using namespace knotwork;
  const std::string name = "generated_case_0394";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_394", {0.500f, 1.000f, 0.333f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_394", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_394", root, mat, trs({10.000f, 0.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_394", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_394", root, LightKind::Point, 15.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_394", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 11.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_394") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_394";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0395() {
  using namespace knotwork;
  const std::string name = "generated_case_0395";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_395", {1.000f, 0.000f, 0.500f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_395", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_395", root, mat, trs({11.000f, 0.750f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_395", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_395", root, LightKind::Point, 16.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_395", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 13.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_395") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_395";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0396() {
  using namespace knotwork;
  const std::string name = "generated_case_0396";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_396", {0.000f, 0.250f, 0.667f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_396", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_396", root, mat, trs({1.000f, 1.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_396", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_396", root, LightKind::Point, 17.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_396", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 6.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_396") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({6.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_396";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 7.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0397() {
  using namespace knotwork;
  const std::string name = "generated_case_0397";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_397", {0.500f, 0.500f, 0.833f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_397", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_397", root, mat, trs({2.000f, 1.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_397", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_397", root, LightKind::Point, 18.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_397", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 6.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_397") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_397";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0398() {
  using namespace knotwork;
  const std::string name = "generated_case_0398";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_398", {1.000f, 0.750f, 1.000f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_398", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_398", root, mat, trs({3.000f, 1.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_398", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_398", root, LightKind::Point, 19.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_398", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_398") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_398";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0399() {
  using namespace knotwork;
  const std::string name = "generated_case_0399";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_399", {0.000f, 1.000f, 0.000f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_399", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_399", root, mat, trs({4.000f, 0.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_399", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_399", root, LightKind::Point, 20.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_399", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 10.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_399") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_399";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0400() {
  using namespace knotwork;
  const std::string name = "generated_case_0400";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_400", {0.500f, 0.000f, 0.167f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_400", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_400", root, mat, trs({5.000f, 0.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_400", root, 55.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_400", root, LightKind::Point, 1.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_400", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 12.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_400") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_400";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0401() {
  using namespace knotwork;
  const std::string name = "generated_case_0401";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_401", {1.000f, 0.250f, 0.333f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_401", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_401", root, mat, trs({6.000f, 0.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_401", root, 56.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_401", root, LightKind::Point, 2.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_401", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 14.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_401") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_401";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0402() {
  using namespace knotwork;
  const std::string name = "generated_case_0402";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_402", {0.000f, 0.500f, 0.500f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_402", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_402", root, mat, trs({7.000f, 0.750f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_402", root, 57.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_402", root, LightKind::Point, 3.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_402", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 16.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_402") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_402";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0403() {
  using namespace knotwork;
  const std::string name = "generated_case_0403";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_403", {0.500f, 0.750f, 0.667f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_403", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_403", root, mat, trs({8.000f, 1.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_403", root, 58.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_403", root, LightKind::Point, 4.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_403", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 31.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_403") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_403";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0404() {
  using namespace knotwork;
  const std::string name = "generated_case_0404";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_404", {1.000f, 1.000f, 0.833f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_404", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_404", root, mat, trs({9.000f, 1.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_404", root, 59.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_404", root, LightKind::Point, 5.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_404", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 22.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 33.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_404") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({22.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_404";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 23.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0405() {
  using namespace knotwork;
  const std::string name = "generated_case_0405";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_405", {0.000f, 0.000f, 1.000f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_405", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_405", root, mat, trs({10.000f, 1.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_405", root, 60.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_405", root, LightKind::Point, 6.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_405", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 24.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 35.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_405") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({24.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_405";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 25.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0406() {
  using namespace knotwork;
  const std::string name = "generated_case_0406";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_406", {0.500f, 0.250f, 0.000f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_406", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_406", root, mat, trs({11.000f, 0.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_406", root, 61.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_406", root, LightKind::Point, 7.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_406", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 26.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 37.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_406") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({26.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_406";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 27.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0407() {
  using namespace knotwork;
  const std::string name = "generated_case_0407";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_407", {1.000f, 0.500f, 0.167f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_407", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_407", root, mat, trs({1.000f, 0.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_407", root, 62.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_407", root, LightKind::Point, 8.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_407", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 28.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_407") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_407";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0408() {
  using namespace knotwork;
  const std::string name = "generated_case_0408";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_408", {0.000f, 0.750f, 0.333f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_408", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_408", root, mat, trs({2.000f, 0.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_408", root, 63.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_408", root, LightKind::Point, 9.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_408", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 2.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_408") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({2.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_408";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 3.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0409() {
  using namespace knotwork;
  const std::string name = "generated_case_0409";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_409", {0.500f, 1.000f, 0.500f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_409", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_409", root, mat, trs({3.000f, 0.750f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_409", root, 64.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_409", root, LightKind::Point, 10.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_409", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 4.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -2.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_409") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({4.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_409";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 5.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0410() {
  using namespace knotwork;
  const std::string name = "generated_case_0410";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_410", {1.000f, 0.000f, 0.667f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_410", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_410", root, mat, trs({4.000f, 1.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_410", root, 65.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_410", root, LightKind::Point, 11.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_410", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 6.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 0.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_410") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({6.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_410";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 7.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0411() {
  using namespace knotwork;
  const std::string name = "generated_case_0411";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_411", {0.000f, 0.250f, 0.833f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_411", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_411", root, mat, trs({5.000f, 1.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_411", root, 66.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_411", root, LightKind::Point, 12.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_411", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 2.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_411") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_411";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0412() {
  using namespace knotwork;
  const std::string name = "generated_case_0412";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_412", {0.500f, 0.500f, 1.000f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_412", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_412", root, mat, trs({6.000f, 1.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_412", root, 67.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_412", root, LightKind::Point, 13.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_412", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_412") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_412";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0413() {
  using namespace knotwork;
  const std::string name = "generated_case_0413";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_413", {1.000f, 0.750f, 0.000f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_413", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_413", root, mat, trs({7.000f, 0.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_413", root, 68.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_413", root, LightKind::Point, 14.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_413", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 6.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_413") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_413";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0414() {
  using namespace knotwork;
  const std::string name = "generated_case_0414";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_414", {0.000f, 1.000f, 0.167f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_414", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_414", root, mat, trs({8.000f, 0.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_414", root, 69.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_414", root, LightKind::Point, 15.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_414", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_414") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_414";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0415() {
  using namespace knotwork;
  const std::string name = "generated_case_0415";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_415", {0.500f, 0.000f, 0.333f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_415", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_415", root, mat, trs({9.000f, 0.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_415", root, 70.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_415", root, LightKind::Point, 16.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_415", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 10.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_415") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_415";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0416() {
  using namespace knotwork;
  const std::string name = "generated_case_0416";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_416", {1.000f, 0.250f, 0.500f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_416", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_416", root, mat, trs({10.000f, 0.750f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_416", root, 71.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_416", root, LightKind::Point, 17.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_416", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 25.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_416") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_416";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0417() {
  using namespace knotwork;
  const std::string name = "generated_case_0417";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_417", {0.000f, 0.500f, 0.667f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_417", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_417", root, mat, trs({11.000f, 1.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_417", root, 72.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_417", root, LightKind::Point, 18.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_417", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 27.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_417") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_417";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0418() {
  using namespace knotwork;
  const std::string name = "generated_case_0418";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_418", {0.500f, 0.750f, 0.833f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_418", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_418", root, mat, trs({1.000f, 1.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_418", root, 73.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_418", root, LightKind::Point, 19.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_418", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 18.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_418") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_418";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0419() {
  using namespace knotwork;
  const std::string name = "generated_case_0419";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_419", {1.000f, 1.000f, 1.000f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_419", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_419", root, mat, trs({2.000f, 1.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_419", root, 74.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_419", root, LightKind::Point, 20.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_419", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 20.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_419") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_419";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0420() {
  using namespace knotwork;
  const std::string name = "generated_case_0420";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_420", {0.000f, 0.000f, 0.000f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_420", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_420", root, mat, trs({3.000f, 0.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_420", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_420", root, LightKind::Point, 1.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_420", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 22.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_420") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_420";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0421() {
  using namespace knotwork;
  const std::string name = "generated_case_0421";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_421", {0.500f, 0.250f, 0.167f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_421", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_421", root, mat, trs({4.000f, 0.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_421", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_421", root, LightKind::Point, 2.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_421", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 24.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_421") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_421";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0422() {
  using namespace knotwork;
  const std::string name = "generated_case_0422";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_422", {1.000f, 0.500f, 0.333f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_422", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_422", root, mat, trs({5.000f, 0.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_422", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_422", root, LightKind::Point, 3.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_422", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 26.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_422") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_422";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0423() {
  using namespace knotwork;
  const std::string name = "generated_case_0423";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_423", {0.000f, 0.750f, 0.500f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_423", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_423", root, mat, trs({6.000f, 0.750f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_423", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_423", root, LightKind::Point, 4.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_423", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 28.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_423") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_423";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0424() {
  using namespace knotwork;
  const std::string name = "generated_case_0424";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_424", {0.500f, 1.000f, 0.667f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_424", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_424", root, mat, trs({7.000f, 1.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_424", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_424", root, LightKind::Point, 5.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_424", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 23.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 30.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_424") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({23.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_424";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 24.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0425() {
  using namespace knotwork;
  const std::string name = "generated_case_0425";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_425", {1.000f, 0.000f, 0.833f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_425", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_425", root, mat, trs({8.000f, 1.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_425", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_425", root, LightKind::Point, 6.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_425", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -2.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_425") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_425";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0426() {
  using namespace knotwork;
  const std::string name = "generated_case_0426";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_426", {0.000f, 0.250f, 1.000f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_426", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_426", root, mat, trs({9.000f, 1.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_426", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_426", root, LightKind::Point, 7.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_426", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 0.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_426") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_426";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0427() {
  using namespace knotwork;
  const std::string name = "generated_case_0427";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_427", {0.500f, 0.500f, 0.000f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_427", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_427", root, mat, trs({10.000f, 0.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_427", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_427", root, LightKind::Point, 8.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_427", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 2.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_427") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_427";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0428() {
  using namespace knotwork;
  const std::string name = "generated_case_0428";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_428", {1.000f, 0.750f, 0.167f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_428", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_428", root, mat, trs({11.000f, 0.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_428", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_428", root, LightKind::Point, 9.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_428", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_428") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_428";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0429() {
  using namespace knotwork;
  const std::string name = "generated_case_0429";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_429", {0.000f, 1.000f, 0.333f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_429", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_429", root, mat, trs({1.000f, 0.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_429", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_429", root, LightKind::Point, 10.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_429", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 5.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_429") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({5.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_429";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 6.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0430() {
  using namespace knotwork;
  const std::string name = "generated_case_0430";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_430", {0.500f, 0.000f, 0.500f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_430", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_430", root, mat, trs({2.000f, 0.750f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_430", root, 55.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_430", root, LightKind::Point, 11.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_430", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 10.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_430") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_430";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0431() {
  using namespace knotwork;
  const std::string name = "generated_case_0431";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_431", {1.000f, 0.250f, 0.667f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_431", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_431", root, mat, trs({3.000f, 1.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_431", root, 56.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_431", root, LightKind::Point, 12.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_431", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 12.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_431") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_431";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0432() {
  using namespace knotwork;
  const std::string name = "generated_case_0432";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_432", {0.000f, 0.500f, 0.833f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_432", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_432", root, mat, trs({4.000f, 1.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_432", root, 57.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_432", root, LightKind::Point, 13.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_432", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 14.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_432") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_432";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0433() {
  using namespace knotwork;
  const std::string name = "generated_case_0433";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_433", {0.500f, 0.750f, 1.000f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_433", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_433", root, mat, trs({5.000f, 1.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_433", root, 58.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_433", root, LightKind::Point, 14.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_433", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 16.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_433") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_433";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0434() {
  using namespace knotwork;
  const std::string name = "generated_case_0434";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_434", {1.000f, 1.000f, 0.000f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_434", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_434", root, mat, trs({6.000f, 0.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_434", root, 59.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_434", root, LightKind::Point, 15.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_434", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 18.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_434") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_434";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0435() {
  using namespace knotwork;
  const std::string name = "generated_case_0435";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_435", {0.000f, 0.000f, 0.167f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_435", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_435", root, mat, trs({7.000f, 0.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_435", root, 60.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_435", root, LightKind::Point, 16.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_435", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 20.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_435") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_435";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0436() {
  using namespace knotwork;
  const std::string name = "generated_case_0436";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_436", {0.500f, 0.250f, 0.333f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_436", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_436", root, mat, trs({8.000f, 0.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_436", root, 61.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_436", root, LightKind::Point, 17.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_436", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 22.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_436") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_436";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0437() {
  using namespace knotwork;
  const std::string name = "generated_case_0437";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_437", {1.000f, 0.500f, 0.500f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_437", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_437", root, mat, trs({9.000f, 0.750f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_437", root, 62.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_437", root, LightKind::Point, 18.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_437", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 24.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_437") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_437";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0438() {
  using namespace knotwork;
  const std::string name = "generated_case_0438";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_438", {0.000f, 0.750f, 0.667f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_438", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_438", root, mat, trs({10.000f, 1.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_438", root, 63.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_438", root, LightKind::Point, 19.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_438", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 23.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 26.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_438") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({23.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_438";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 24.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0439() {
  using namespace knotwork;
  const std::string name = "generated_case_0439";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_439", {0.500f, 1.000f, 0.833f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_439", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_439", root, mat, trs({11.000f, 1.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_439", root, 64.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_439", root, LightKind::Point, 20.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_439", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 25.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 28.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_439") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({25.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_439";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 26.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0440() {
  using namespace knotwork;
  const std::string name = "generated_case_0440";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_440", {1.000f, 0.000f, 1.000f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_440", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_440", root, mat, trs({1.000f, 1.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_440", root, 65.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_440", root, LightKind::Point, 1.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_440", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 19.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_440") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_440";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0441() {
  using namespace knotwork;
  const std::string name = "generated_case_0441";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_441", {0.000f, 0.250f, 0.000f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_441", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_441", root, mat, trs({2.000f, 0.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_441", root, 66.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_441", root, LightKind::Point, 2.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_441", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 21.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_441") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_441";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0442() {
  using namespace knotwork;
  const std::string name = "generated_case_0442";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_442", {0.500f, 0.500f, 0.167f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_442", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_442", root, mat, trs({3.000f, 0.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_442", root, 67.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_442", root, LightKind::Point, 3.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_442", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 3.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 2.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_442") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({3.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_442";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 4.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0443() {
  using namespace knotwork;
  const std::string name = "generated_case_0443";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_443", {1.000f, 0.750f, 0.333f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_443", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_443", root, mat, trs({4.000f, 0.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_443", root, 68.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_443", root, LightKind::Point, 4.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_443", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 5.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_443") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({5.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_443";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 6.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0444() {
  using namespace knotwork;
  const std::string name = "generated_case_0444";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_444", {0.000f, 1.000f, 0.500f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_444", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_444", root, mat, trs({5.000f, 0.750f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_444", root, 69.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_444", root, LightKind::Point, 5.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_444", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 6.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_444") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_444";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0445() {
  using namespace knotwork;
  const std::string name = "generated_case_0445";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_445", {0.500f, 0.000f, 0.667f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_445", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_445", root, mat, trs({6.000f, 1.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_445", root, 70.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_445", root, LightKind::Point, 6.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_445", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_445") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_445";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0446() {
  using namespace knotwork;
  const std::string name = "generated_case_0446";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_446", {1.000f, 0.250f, 0.833f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_446", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_446", root, mat, trs({7.000f, 1.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_446", root, 71.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_446", root, LightKind::Point, 7.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_446", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 10.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_446") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_446";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0447() {
  using namespace knotwork;
  const std::string name = "generated_case_0447";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_447", {0.000f, 0.500f, 1.000f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_447", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_447", root, mat, trs({8.000f, 1.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_447", root, 72.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_447", root, LightKind::Point, 8.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_447", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 12.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_447") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_447";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0448() {
  using namespace knotwork;
  const std::string name = "generated_case_0448";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_448", {0.500f, 0.750f, 0.000f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_448", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_448", root, mat, trs({9.000f, 0.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_448", root, 73.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_448", root, LightKind::Point, 9.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_448", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 14.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_448") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_448";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0449() {
  using namespace knotwork;
  const std::string name = "generated_case_0449";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_449", {1.000f, 1.000f, 0.167f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_449", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_449", root, mat, trs({10.000f, 0.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_449", root, 74.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_449", root, LightKind::Point, 10.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_449", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 16.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_449") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_449";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0450() {
  using namespace knotwork;
  const std::string name = "generated_case_0450";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_450", {0.000f, 0.000f, 0.333f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_450", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_450", root, mat, trs({11.000f, 0.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_450", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_450", root, LightKind::Point, 11.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_450", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 18.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_450") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_450";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0451() {
  using namespace knotwork;
  const std::string name = "generated_case_0451";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_451", {0.500f, 0.250f, 0.500f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_451", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_451", root, mat, trs({1.000f, 0.750f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_451", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_451", root, LightKind::Point, 12.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_451", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_451") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_451";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0452() {
  using namespace knotwork;
  const std::string name = "generated_case_0452";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_452", {1.000f, 0.500f, 0.667f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_452", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_452", root, mat, trs({2.000f, 1.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_452", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_452", root, LightKind::Point, 13.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_452", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 11.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_452") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_452";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0453() {
  using namespace knotwork;
  const std::string name = "generated_case_0453";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_453", {0.000f, 0.750f, 0.833f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_453", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_453", root, mat, trs({3.000f, 1.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_453", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_453", root, LightKind::Point, 14.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_453", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 13.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_453") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_453";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0454() {
  using namespace knotwork;
  const std::string name = "generated_case_0454";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_454", {0.500f, 1.000f, 1.000f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_454", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_454", root, mat, trs({4.000f, 1.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_454", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_454", root, LightKind::Point, 15.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_454", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 15.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_454") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_454";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0455() {
  using namespace knotwork;
  const std::string name = "generated_case_0455";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_455", {1.000f, 0.000f, 0.000f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_455", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_455", root, mat, trs({5.000f, 0.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_455", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_455", root, LightKind::Point, 16.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_455", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 30.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_455") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_455";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0456() {
  using namespace knotwork;
  const std::string name = "generated_case_0456";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_456", {0.000f, 0.250f, 0.167f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_456", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_456", root, mat, trs({6.000f, 0.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_456", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_456", root, LightKind::Point, 17.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_456", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 32.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_456") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_456";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0457() {
  using namespace knotwork;
  const std::string name = "generated_case_0457";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_457", {0.500f, 0.500f, 0.333f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_457", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_457", root, mat, trs({7.000f, 0.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_457", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_457", root, LightKind::Point, 18.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_457", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 22.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 34.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_457") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({22.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_457";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 23.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0458() {
  using namespace knotwork;
  const std::string name = "generated_case_0458";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_458", {1.000f, 0.750f, 0.500f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_458", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_458", root, mat, trs({8.000f, 0.750f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_458", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_458", root, LightKind::Point, 19.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_458", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 24.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 36.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_458") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({24.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_458";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 25.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0459() {
  using namespace knotwork;
  const std::string name = "generated_case_0459";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_459", {0.000f, 1.000f, 0.667f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_459", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_459", root, mat, trs({9.000f, 1.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_459", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_459", root, LightKind::Point, 20.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_459", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_459") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_459";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0460() {
  using namespace knotwork;
  const std::string name = "generated_case_0460";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_460", {0.500f, 0.000f, 0.833f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_460", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_460", root, mat, trs({10.000f, 1.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_460", root, 55.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_460", root, LightKind::Point, 1.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_460", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 6.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_460") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_460";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0461() {
  using namespace knotwork;
  const std::string name = "generated_case_0461";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_461", {1.000f, 0.250f, 1.000f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_461", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_461", root, mat, trs({11.000f, 1.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_461", root, 56.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_461", root, LightKind::Point, 2.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_461", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_461") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_461";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0462() {
  using namespace knotwork;
  const std::string name = "generated_case_0462";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_462", {0.000f, 0.500f, 0.000f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_462", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_462", root, mat, trs({1.000f, 0.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_462", root, 57.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_462", root, LightKind::Point, 3.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_462", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 4.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_462") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({4.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_462";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 5.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0463() {
  using namespace knotwork;
  const std::string name = "generated_case_0463";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_463", {0.500f, 0.750f, 0.167f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_463", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_463", root, mat, trs({2.000f, 0.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_463", root, 58.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_463", root, LightKind::Point, 4.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_463", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 6.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_463") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({6.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_463";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 7.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0464() {
  using namespace knotwork;
  const std::string name = "generated_case_0464";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_464", {1.000f, 1.000f, 0.333f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_464", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_464", root, mat, trs({3.000f, 0.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_464", root, 59.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_464", root, LightKind::Point, 5.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_464", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_464") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_464";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0465() {
  using namespace knotwork;
  const std::string name = "generated_case_0465";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_465", {0.000f, 0.000f, 0.500f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_465", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_465", root, mat, trs({4.000f, 0.750f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_465", root, 60.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_465", root, LightKind::Point, 6.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_465", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_465") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_465";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0466() {
  using namespace knotwork;
  const std::string name = "generated_case_0466";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_466", {0.500f, 0.250f, 0.667f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_466", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_466", root, mat, trs({5.000f, 1.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_466", root, 61.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_466", root, LightKind::Point, 7.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_466", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_466") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_466";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0467() {
  using namespace knotwork;
  const std::string name = "generated_case_0467";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_467", {1.000f, 0.500f, 0.833f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_467", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_467", root, mat, trs({6.000f, 1.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_467", root, 62.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_467", root, LightKind::Point, 8.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_467", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_467") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_467";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0468() {
  using namespace knotwork;
  const std::string name = "generated_case_0468";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_468", {0.000f, 0.750f, 1.000f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_468", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_468", root, mat, trs({7.000f, 1.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_468", root, 63.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_468", root, LightKind::Point, 9.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_468", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 24.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_468") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_468";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0469() {
  using namespace knotwork;
  const std::string name = "generated_case_0469";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_469", {0.500f, 1.000f, 0.000f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_469", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_469", root, mat, trs({8.000f, 0.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_469", root, 64.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_469", root, LightKind::Point, 10.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_469", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 26.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_469") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_469";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0470() {
  using namespace knotwork;
  const std::string name = "generated_case_0470";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_470", {1.000f, 0.000f, 0.167f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_470", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_470", root, mat, trs({9.000f, 0.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_470", root, 65.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_470", root, LightKind::Point, 11.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_470", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 28.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_470") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_470";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0471() {
  using namespace knotwork;
  const std::string name = "generated_case_0471";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_471", {0.000f, 0.250f, 0.333f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_471", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_471", root, mat, trs({10.000f, 0.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_471", root, 66.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_471", root, LightKind::Point, 12.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_471", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 22.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 30.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_471") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({22.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_471";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 23.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0472() {
  using namespace knotwork;
  const std::string name = "generated_case_0472";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_472", {0.500f, 0.500f, 0.500f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_472", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_472", root, mat, trs({11.000f, 0.750f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_472", root, 67.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_472", root, LightKind::Point, 13.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_472", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 24.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 32.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_472") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({24.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_472";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 25.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0473() {
  using namespace knotwork;
  const std::string name = "generated_case_0473";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_473", {1.000f, 0.750f, 0.667f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_473", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_473", root, mat, trs({1.000f, 1.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_473", root, 68.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_473", root, LightKind::Point, 14.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_473", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 23.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_473") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_473";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0474() {
  using namespace knotwork;
  const std::string name = "generated_case_0474";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_474", {0.000f, 1.000f, 0.833f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_474", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_474", root, mat, trs({2.000f, 1.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_474", root, 69.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_474", root, LightKind::Point, 15.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_474", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 25.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_474") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_474";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0475() {
  using namespace knotwork;
  const std::string name = "generated_case_0475";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_475", {0.500f, 0.000f, 1.000f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_475", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_475", root, mat, trs({3.000f, 1.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_475", root, 70.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_475", root, LightKind::Point, 16.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_475", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 27.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_475") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_475";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0476() {
  using namespace knotwork;
  const std::string name = "generated_case_0476";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_476", {1.000f, 0.250f, 0.000f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_476", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_476", root, mat, trs({4.000f, 0.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_476", root, 71.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_476", root, LightKind::Point, 17.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_476", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 4.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_476") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({4.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_476";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 5.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0477() {
  using namespace knotwork;
  const std::string name = "generated_case_0477";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_477", {0.000f, 0.500f, 0.167f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_477", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_477", root, mat, trs({5.000f, 0.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_477", root, 72.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_477", root, LightKind::Point, 18.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_477", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 6.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_477") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({6.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_477";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 7.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0478() {
  using namespace knotwork;
  const std::string name = "generated_case_0478";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_478", {0.500f, 0.750f, 0.333f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_478", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_478", root, mat, trs({6.000f, 0.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_478", root, 73.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_478", root, LightKind::Point, 19.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_478", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_478") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_478";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0479() {
  using namespace knotwork;
  const std::string name = "generated_case_0479";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_479", {1.000f, 1.000f, 0.500f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_479", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_479", root, mat, trs({7.000f, 0.750f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_479", root, 74.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_479", root, LightKind::Point, 20.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_479", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_479") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_479";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0480() {
  using namespace knotwork;
  const std::string name = "generated_case_0480";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_480", {0.000f, 0.000f, 0.667f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_480", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_480", root, mat, trs({8.000f, 1.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_480", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_480", root, LightKind::Point, 1.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_480", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_480") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_480";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0481() {
  using namespace knotwork;
  const std::string name = "generated_case_0481";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_481", {0.500f, 0.250f, 0.833f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_481", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_481", root, mat, trs({9.000f, 1.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_481", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_481", root, LightKind::Point, 2.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_481", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 18.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_481") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_481";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0482() {
  using namespace knotwork;
  const std::string name = "generated_case_0482";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_482", {1.000f, 0.500f, 1.000f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_482", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_482", root, mat, trs({10.000f, 1.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_482", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_482", root, LightKind::Point, 3.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_482", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 20.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_482") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_482";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0483() {
  using namespace knotwork;
  const std::string name = "generated_case_0483";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_483", {0.000f, 0.750f, 0.000f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_483", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_483", root, mat, trs({11.000f, 0.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_483", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_483", root, LightKind::Point, 4.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_483", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 22.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_483") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_483";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0484() {
  using namespace knotwork;
  const std::string name = "generated_case_0484";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_484", {0.500f, 1.000f, 0.167f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_484", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_484", root, mat, trs({1.000f, 0.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_484", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_484", root, LightKind::Point, 5.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_484", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 13.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_484") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_484";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0485() {
  using namespace knotwork;
  const std::string name = "generated_case_0485";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_485", {1.000f, 0.000f, 0.333f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_485", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_485", root, mat, trs({2.000f, 0.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_485", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_485", root, LightKind::Point, 6.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_485", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 15.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_485") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_485";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0486() {
  using namespace knotwork;
  const std::string name = "generated_case_0486";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_486", {0.000f, 0.250f, 0.500f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_486", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_486", root, mat, trs({3.000f, 0.750f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_486", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_486", root, LightKind::Point, 7.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_486", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 17.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_486") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_486";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0487() {
  using namespace knotwork;
  const std::string name = "generated_case_0487";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_487", {0.500f, 0.500f, 0.667f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_487", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_487", root, mat, trs({4.000f, 1.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_487", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_487", root, LightKind::Point, 8.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_487", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 19.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_487") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_487";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0488() {
  using namespace knotwork;
  const std::string name = "generated_case_0488";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_488", {1.000f, 0.750f, 0.833f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_488", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_488", root, mat, trs({5.000f, 1.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_488", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_488", root, LightKind::Point, 9.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_488", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 21.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_488") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_488";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0489() {
  using namespace knotwork;
  const std::string name = "generated_case_0489";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_489", {0.000f, 1.000f, 1.000f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_489", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_489", root, mat, trs({6.000f, 1.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_489", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_489", root, LightKind::Point, 10.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_489", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 23.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_489") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_489";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0490() {
  using namespace knotwork;
  const std::string name = "generated_case_0490";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_490", {0.500f, 0.000f, 0.000f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_490", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_490", root, mat, trs({7.000f, 0.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_490", root, 55.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_490", root, LightKind::Point, 11.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_490", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 25.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_490") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_490";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0491() {
  using namespace knotwork;
  const std::string name = "generated_case_0491";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_491", {1.000f, 0.250f, 0.167f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_491", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_491", root, mat, trs({8.000f, 0.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_491", root, 56.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_491", root, LightKind::Point, 12.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_491", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 23.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 27.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_491") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({23.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_491";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 24.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0492() {
  using namespace knotwork;
  const std::string name = "generated_case_0492";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_492", {0.000f, 0.500f, 0.333f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_492", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_492", root, mat, trs({9.000f, 0.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_492", root, 57.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_492", root, LightKind::Point, 13.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_492", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 25.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 29.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_492") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({25.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_492";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 26.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0493() {
  using namespace knotwork;
  const std::string name = "generated_case_0493";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_493", {0.500f, 0.750f, 0.500f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_493", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_493", root, mat, trs({10.000f, 0.750f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_493", root, 58.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_493", root, LightKind::Point, 14.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_493", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), -3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_493") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_493";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0494() {
  using namespace knotwork;
  const std::string name = "generated_case_0494";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_494", {1.000f, 1.000f, 0.667f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_494", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_494", root, mat, trs({11.000f, 1.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_494", root, 59.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_494", root, LightKind::Point, 15.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_494", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 12.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_494") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_494";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0495() {
  using namespace knotwork;
  const std::string name = "generated_case_0495";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_495", {0.000f, 0.000f, 0.833f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_495", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_495", root, mat, trs({1.000f, 1.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_495", root, 60.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_495", root, LightKind::Point, 16.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_495", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 3.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_495") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({3.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_495";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 4.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0496() {
  using namespace knotwork;
  const std::string name = "generated_case_0496";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_496", {0.500f, 0.250f, 1.000f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_496", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_496", root, mat, trs({2.000f, 1.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_496", root, 61.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_496", root, LightKind::Point, 17.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_496", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 5.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_496") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({5.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_496";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 6.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0497() {
  using namespace knotwork;
  const std::string name = "generated_case_0497";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_497", {1.000f, 0.500f, 0.000f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_497", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_497", root, mat, trs({3.000f, 0.000f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_497", root, 62.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_497", root, LightKind::Point, 18.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_497", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_497") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_497";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0498() {
  using namespace knotwork;
  const std::string name = "generated_case_0498";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_498", {0.000f, 0.750f, 0.167f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_498", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_498", root, mat, trs({4.000f, 0.250f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_498", root, 63.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_498", root, LightKind::Point, 19.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_498", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_498") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_498";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0499() {
  using namespace knotwork;
  const std::string name = "generated_case_0499";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_499", {0.500f, 1.000f, 0.333f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_499", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_499", root, mat, trs({5.000f, 0.500f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_499", root, 64.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_499", root, LightKind::Point, 20.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_499", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 11.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_499") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_499";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0500() {
  using namespace knotwork;
  const std::string name = "generated_case_0500";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_500", {1.000f, 0.000f, 0.500f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_500", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_500", root, mat, trs({6.000f, 0.750f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_500", root, 65.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_500", root, LightKind::Point, 1.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_500", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 13.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_500") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_500";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0501() {
  using namespace knotwork;
  const std::string name = "generated_case_0501";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_501", {0.000f, 0.250f, 0.667f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_501", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_501", root, mat, trs({7.000f, 1.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_501", root, 66.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_501", root, LightKind::Point, 2.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_501", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 15.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_501") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_501";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0502() {
  using namespace knotwork;
  const std::string name = "generated_case_0502";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_502", {0.500f, 0.500f, 0.833f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_502", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_502", root, mat, trs({8.000f, 1.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_502", root, 67.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_502", root, LightKind::Point, 3.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_502", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 17.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_502") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_502";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0503() {
  using namespace knotwork;
  const std::string name = "generated_case_0503";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_503", {1.000f, 0.750f, 1.000f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_503", NodeKind::Empty, kNoIndex, translated(10.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_503", root, mat, trs({9.000f, 1.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_503", root, 68.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_503", root, LightKind::Point, 4.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_503", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 19.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 19.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_503") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({19.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_503";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 20.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0504() {
  using namespace knotwork;
  const std::string name = "generated_case_0504";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_504", {0.000f, 1.000f, 0.000f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_504", NodeKind::Empty, kNoIndex, translated(11.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_504", root, mat, trs({10.000f, 0.000f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_504", root, 69.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_504", root, LightKind::Point, 5.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_504", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 21.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 21.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_504") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({21.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_504";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 22.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0505() {
  using namespace knotwork;
  const std::string name = "generated_case_0505";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_505", {0.500f, 0.000f, 0.167f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_505", NodeKind::Empty, kNoIndex, translated(12.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_505", root, mat, trs({11.000f, 0.250f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_505", root, 70.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_505", root, LightKind::Point, 6.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_505", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 23.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 23.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_505") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({23.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_505";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 24.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0506() {
  using namespace knotwork;
  const std::string name = "generated_case_0506";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_506", {1.000f, 0.250f, 0.333f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_506", NodeKind::Empty, kNoIndex, translated(13.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_506", root, mat, trs({1.000f, 0.500f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_506", root, 71.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_506", root, LightKind::Point, 7.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_506", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 14.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 14.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_506") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({14.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_506";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 15.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0507() {
  using namespace knotwork;
  const std::string name = "generated_case_0507";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_507", {0.000f, 0.500f, 0.500f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_507", NodeKind::Empty, kNoIndex, translated(14.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_507", root, mat, trs({2.000f, 0.750f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_507", root, 72.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_507", root, LightKind::Point, 8.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_507", root, mesh, translated(-1.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 16.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 29.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_507") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({16.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_507";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 17.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0508() {
  using namespace knotwork;
  const std::string name = "generated_case_0508";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_508", {0.500f, 0.750f, 0.667f}, 0.600f, 0.000f);
  const std::uint32_t root = builder.add_node("root_508", NodeKind::Empty, kNoIndex, translated(15.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_508", root, mat, trs({3.000f, 1.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_508", root, 73.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_508", root, LightKind::Point, 9.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_508", root, mesh, translated(-2.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 18.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 31.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_508") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({18.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_508";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 19.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0509() {
  using namespace knotwork;
  const std::string name = "generated_case_0509";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_509", {1.000f, 1.000f, 0.833f}, 0.700f, 0.050f);
  const std::uint32_t root = builder.add_node("root_509", NodeKind::Empty, kNoIndex, translated(16.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_509", root, mat, trs({4.000f, 1.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_509", root, 74.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_509", root, LightKind::Point, 10.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_509", root, mesh, translated(-3.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 20.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 33.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_509") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({20.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_509";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 21.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0510() {
  using namespace knotwork;
  const std::string name = "generated_case_0510";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_510", {0.000f, 0.000f, 1.000f}, 0.200f, 0.100f);
  const std::uint32_t root = builder.add_node("root_510", NodeKind::Empty, kNoIndex, translated(0.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_510", root, mat, trs({5.000f, 1.500f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_510", root, 45.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_510", root, LightKind::Point, 11.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_510", root, mesh, translated(-4.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 5.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 1.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_510") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({5.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_510";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 6.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0511() {
  using namespace knotwork;
  const std::string name = "generated_case_0511";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_511", {0.500f, 0.250f, 0.000f}, 0.300f, 0.150f);
  const std::uint32_t root = builder.add_node("root_511", NodeKind::Empty, kNoIndex, translated(1.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_511", root, mat, trs({6.000f, 0.000f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_511", root, 46.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_511", root, LightKind::Point, 12.000f, translated(0.0f, 9.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_511", root, mesh, translated(-5.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 7.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 3.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_511") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({7.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_511";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(6.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 8.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0512() {
  using namespace knotwork;
  const std::string name = "generated_case_0512";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_512", {1.000f, 0.500f, 0.167f}, 0.400f, 0.000f);
  const std::uint32_t root = builder.add_node("root_512", NodeKind::Empty, kNoIndex, translated(2.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_512", root, mat, trs({7.000f, 0.250f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_512", root, 47.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_512", root, LightKind::Point, 13.000f, translated(0.0f, 10.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_512", root, mesh, translated(-6.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 9.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 5.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_512") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({9.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_512";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(7.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 10.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0513() {
  using namespace knotwork;
  const std::string name = "generated_case_0513";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_513", {0.000f, 0.750f, 0.333f}, 0.500f, 0.050f);
  const std::uint32_t root = builder.add_node("root_513", NodeKind::Empty, kNoIndex, translated(3.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_513", root, mat, trs({8.000f, 0.500f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_513", root, 48.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_513", root, LightKind::Point, 14.000f, translated(0.0f, 2.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_513", root, mesh, translated(-7.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 11.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 7.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_513") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({11.000f, 0.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_513";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(8.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 12.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0514() {
  using namespace knotwork;
  const std::string name = "generated_case_0514";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_514", {0.500f, 1.000f, 0.500f}, 0.600f, 0.100f);
  const std::uint32_t root = builder.add_node("root_514", NodeKind::Empty, kNoIndex, translated(4.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_514", root, mat, trs({9.000f, 0.750f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_514", root, 49.000f, translated(0.0f, 2.0f, 8.000f));
  const std::uint32_t light = builder.add_light("light_514", root, LightKind::Point, 15.000f, translated(0.0f, 3.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_514", root, mesh, translated(-8.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 13.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 9.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_514") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({13.000f, 0.750f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_514";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(9.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 14.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0515() {
  using namespace knotwork;
  const std::string name = "generated_case_0515";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_515", {1.000f, 0.000f, 0.667f}, 0.700f, 0.150f);
  const std::uint32_t root = builder.add_node("root_515", NodeKind::Empty, kNoIndex, translated(5.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_515", root, mat, trs({10.000f, 1.000f, 0.0f}, {}, {1.000f, 1.000f, 1.000f}));
  const std::uint32_t camera = builder.add_camera("camera_515", root, 50.000f, translated(0.0f, 2.0f, 9.000f));
  const std::uint32_t light = builder.add_light("light_515", root, LightKind::Point, 16.000f, translated(0.0f, 4.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_515", root, mesh, translated(-9.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 15.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 11.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_515") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({15.000f, 1.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_515";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(10.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(12.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 16.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0516() {
  using namespace knotwork;
  const std::string name = "generated_case_0516";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_516", {0.000f, 0.250f, 0.833f}, 0.200f, 0.000f);
  const std::uint32_t root = builder.add_node("root_516", NodeKind::Empty, kNoIndex, translated(6.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_516", root, mat, trs({11.000f, 1.250f, 0.0f}, {}, {1.100f, 1.100f, 1.100f}));
  const std::uint32_t camera = builder.add_camera("camera_516", root, 51.000f, translated(0.0f, 2.0f, 4.000f));
  const std::uint32_t light = builder.add_light("light_516", root, LightKind::Point, 17.000f, translated(0.0f, 5.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_516", root, mesh, translated(-10.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 17.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 13.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_516") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({17.000f, 1.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_516";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(11.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(13.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 18.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0517() {
  using namespace knotwork;
  const std::string name = "generated_case_0517";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_517", {0.500f, 0.500f, 1.000f}, 0.300f, 0.050f);
  const std::uint32_t root = builder.add_node("root_517", NodeKind::Empty, kNoIndex, translated(7.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_517", root, mat, trs({1.000f, 1.500f, 0.0f}, {}, {1.200f, 1.200f, 1.200f}));
  const std::uint32_t camera = builder.add_camera("camera_517", root, 52.000f, translated(0.0f, 2.0f, 5.000f));
  const std::uint32_t light = builder.add_light("light_517", root, LightKind::Point, 18.000f, translated(0.0f, 6.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_517", root, mesh, translated(-11.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 8.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 4.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_517") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({8.000f, 1.500f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_517";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(1.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 9.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0518() {
  using namespace knotwork;
  const std::string name = "generated_case_0518";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_518", {1.000f, 0.750f, 0.000f}, 0.400f, 0.100f);
  const std::uint32_t root = builder.add_node("root_518", NodeKind::Empty, kNoIndex, translated(8.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_518", root, mat, trs({2.000f, 0.000f, 0.0f}, {}, {1.300f, 1.300f, 1.300f}));
  const std::uint32_t camera = builder.add_camera("camera_518", root, 53.000f, translated(0.0f, 2.0f, 6.000f));
  const std::uint32_t light = builder.add_light("light_518", root, LightKind::Point, 19.000f, translated(0.0f, 7.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_518", root, mesh, translated(-12.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 10.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 6.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_518") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({10.000f, 0.000f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_518";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(2.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(4.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 11.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

void generated_case_0519() {
  using namespace knotwork;
  const std::string name = "generated_case_0519";
  SceneBuilder builder;
  const std::uint32_t mat = builder.add_material("mat_519", {0.000f, 1.000f, 0.167f}, 0.500f, 0.150f);
  const std::uint32_t root = builder.add_node("root_519", NodeKind::Empty, kNoIndex, translated(9.000f, 0.0f, 0.0f));
  const std::uint32_t mesh = builder.add_mesh("mesh_519", root, mat, trs({3.000f, 0.250f, 0.0f}, {}, {1.400f, 1.400f, 1.400f}));
  const std::uint32_t camera = builder.add_camera("camera_519", root, 54.000f, translated(0.0f, 2.0f, 7.000f));
  const std::uint32_t light = builder.add_light("light_519", root, LightKind::Point, 20.000f, translated(0.0f, 8.000f, 1.0f));
  const std::uint32_t inst = builder.add_instance("copy_519", root, mesh, translated(-13.000f, 0.0f, 0.0f));
  Scene scene = builder.take();
  auto loaded = load_scene(save_scene(scene));
  knotwork_generated::check(loaded.ok(), name, loaded.ok() ? "" : loaded.error().message);
  if (!loaded.ok()) return;
  auto flattened = flatten_scene(loaded.value());
  knotwork_generated::check(flattened.ok(), name, flattened.ok() ? "" : flattened.error().message);
  if (!flattened.ok()) return;
  knotwork_generated::check(knotwork_generated::close(flattened.value()[mesh].world.at(0, 3), 12.000f), name, "mesh world x mismatch");
  knotwork_generated::check(knotwork_generated::close(flattened.value()[inst].world.at(0, 3), 8.000f), name, "instance world x mismatch");
  knotwork_generated::check(root_nodes(loaded.value()).size() == 1, name, "root count mismatch");
  knotwork_generated::check(child_nodes(loaded.value(), root).size() == 4, name, "child count mismatch");
  knotwork_generated::check(descendants(loaded.value(), root).size() == 4, name, "descendant count mismatch");
  auto path = path_to_node(loaded.value(), inst);
  knotwork_generated::check(path && path->display.find("copy_519") != std::string::npos, name, "path did not contain instance name");
  NodeQuery mesh_query;
  mesh_query.kind = NodeKind::Mesh;
  knotwork_generated::check(query_nodes(loaded.value(), mesh_query).size() == 1, name, "mesh query mismatch");
  NodeQuery leaf_query;
  leaf_query.leaves_only = true;
  knotwork_generated::check(query_nodes(loaded.value(), leaf_query).size() == 4, name, "leaf query mismatch");
  auto local_bounds = default_unit_mesh_bounds(loaded.value());
  auto bounds = scene_bounds(loaded.value(), local_bounds);
  knotwork_generated::check(bounds.has_value(), name, "missing scene bounds");
  knotwork_generated::check(bounds && bounds->contains({12.000f, 0.250f, 0.0f}), name, "bounds do not contain mesh point");
  AnimationClip clip;
  clip.name = "clip_519";
  clip.duration = 2.0f;
  clip.tracks.push_back(NodeTrack{mesh, {{0.0f, translated(3.000f, 0.0f, 0.0f), Interpolation::Linear}, {2.0f, translated(5.000f, 0.0f, 0.0f), Interpolation::Linear}}});
  knotwork_generated::check(validate_clip(loaded.value(), clip).code == SceneErrorCode::None, name, "clip validation failed");
  Scene animated = apply_clip(loaded.value(), clip, 1.0f);
  auto animated_flat = flatten_scene(animated);
  knotwork_generated::check(animated_flat.ok(), name, "animated flatten failed");
  knotwork_generated::check(animated_flat.ok() && knotwork_generated::close(animated_flat.value()[mesh].world.at(0, 3), 13.000f), name, "animated mesh x mismatch");
  auto diagnostics = diagnose_scene(loaded.value());
  std::string formatted = format_diagnostics(loaded.value(), diagnostics);
  knotwork_generated::check(formatted.find("error") == std::string::npos, name, "diagnostics contained an error");
  std::string text = scene_to_text(loaded.value());
  auto from_text = scene_from_text(text);
  knotwork_generated::check(from_text.ok(), name, from_text.ok() ? "" : from_text.error().message);
  std::string json = scene_to_json(loaded.value());
  knotwork_generated::check(json.find("\"nodes\"") != std::string::npos, name, "json missing nodes");
  (void)camera;
  (void)light;
}

namespace knotwork_generated {

int run_generated_conformance() {
  generated_case_0000();
  generated_case_0001();
  generated_case_0002();
  generated_case_0003();
  generated_case_0004();
  generated_case_0005();
  generated_case_0006();
  generated_case_0007();
  generated_case_0008();
  generated_case_0009();
  generated_case_0010();
  generated_case_0011();
  generated_case_0012();
  generated_case_0013();
  generated_case_0014();
  generated_case_0015();
  generated_case_0016();
  generated_case_0017();
  generated_case_0018();
  generated_case_0019();
  generated_case_0020();
  generated_case_0021();
  generated_case_0022();
  generated_case_0023();
  generated_case_0024();
  generated_case_0025();
  generated_case_0026();
  generated_case_0027();
  generated_case_0028();
  generated_case_0029();
  generated_case_0030();
  generated_case_0031();
  generated_case_0032();
  generated_case_0033();
  generated_case_0034();
  generated_case_0035();
  generated_case_0036();
  generated_case_0037();
  generated_case_0038();
  generated_case_0039();
  generated_case_0040();
  generated_case_0041();
  generated_case_0042();
  generated_case_0043();
  generated_case_0044();
  generated_case_0045();
  generated_case_0046();
  generated_case_0047();
  generated_case_0048();
  generated_case_0049();
  generated_case_0050();
  generated_case_0051();
  generated_case_0052();
  generated_case_0053();
  generated_case_0054();
  generated_case_0055();
  generated_case_0056();
  generated_case_0057();
  generated_case_0058();
  generated_case_0059();
  generated_case_0060();
  generated_case_0061();
  generated_case_0062();
  generated_case_0063();
  generated_case_0064();
  generated_case_0065();
  generated_case_0066();
  generated_case_0067();
  generated_case_0068();
  generated_case_0069();
  generated_case_0070();
  generated_case_0071();
  generated_case_0072();
  generated_case_0073();
  generated_case_0074();
  generated_case_0075();
  generated_case_0076();
  generated_case_0077();
  generated_case_0078();
  generated_case_0079();
  generated_case_0080();
  generated_case_0081();
  generated_case_0082();
  generated_case_0083();
  generated_case_0084();
  generated_case_0085();
  generated_case_0086();
  generated_case_0087();
  generated_case_0088();
  generated_case_0089();
  generated_case_0090();
  generated_case_0091();
  generated_case_0092();
  generated_case_0093();
  generated_case_0094();
  generated_case_0095();
  generated_case_0096();
  generated_case_0097();
  generated_case_0098();
  generated_case_0099();
  generated_case_0100();
  generated_case_0101();
  generated_case_0102();
  generated_case_0103();
  generated_case_0104();
  generated_case_0105();
  generated_case_0106();
  generated_case_0107();
  generated_case_0108();
  generated_case_0109();
  generated_case_0110();
  generated_case_0111();
  generated_case_0112();
  generated_case_0113();
  generated_case_0114();
  generated_case_0115();
  generated_case_0116();
  generated_case_0117();
  generated_case_0118();
  generated_case_0119();
  generated_case_0120();
  generated_case_0121();
  generated_case_0122();
  generated_case_0123();
  generated_case_0124();
  generated_case_0125();
  generated_case_0126();
  generated_case_0127();
  generated_case_0128();
  generated_case_0129();
  generated_case_0130();
  generated_case_0131();
  generated_case_0132();
  generated_case_0133();
  generated_case_0134();
  generated_case_0135();
  generated_case_0136();
  generated_case_0137();
  generated_case_0138();
  generated_case_0139();
  generated_case_0140();
  generated_case_0141();
  generated_case_0142();
  generated_case_0143();
  generated_case_0144();
  generated_case_0145();
  generated_case_0146();
  generated_case_0147();
  generated_case_0148();
  generated_case_0149();
  generated_case_0150();
  generated_case_0151();
  generated_case_0152();
  generated_case_0153();
  generated_case_0154();
  generated_case_0155();
  generated_case_0156();
  generated_case_0157();
  generated_case_0158();
  generated_case_0159();
  generated_case_0160();
  generated_case_0161();
  generated_case_0162();
  generated_case_0163();
  generated_case_0164();
  generated_case_0165();
  generated_case_0166();
  generated_case_0167();
  generated_case_0168();
  generated_case_0169();
  generated_case_0170();
  generated_case_0171();
  generated_case_0172();
  generated_case_0173();
  generated_case_0174();
  generated_case_0175();
  generated_case_0176();
  generated_case_0177();
  generated_case_0178();
  generated_case_0179();
  generated_case_0180();
  generated_case_0181();
  generated_case_0182();
  generated_case_0183();
  generated_case_0184();
  generated_case_0185();
  generated_case_0186();
  generated_case_0187();
  generated_case_0188();
  generated_case_0189();
  generated_case_0190();
  generated_case_0191();
  generated_case_0192();
  generated_case_0193();
  generated_case_0194();
  generated_case_0195();
  generated_case_0196();
  generated_case_0197();
  generated_case_0198();
  generated_case_0199();
  generated_case_0200();
  generated_case_0201();
  generated_case_0202();
  generated_case_0203();
  generated_case_0204();
  generated_case_0205();
  generated_case_0206();
  generated_case_0207();
  generated_case_0208();
  generated_case_0209();
  generated_case_0210();
  generated_case_0211();
  generated_case_0212();
  generated_case_0213();
  generated_case_0214();
  generated_case_0215();
  generated_case_0216();
  generated_case_0217();
  generated_case_0218();
  generated_case_0219();
  generated_case_0220();
  generated_case_0221();
  generated_case_0222();
  generated_case_0223();
  generated_case_0224();
  generated_case_0225();
  generated_case_0226();
  generated_case_0227();
  generated_case_0228();
  generated_case_0229();
  generated_case_0230();
  generated_case_0231();
  generated_case_0232();
  generated_case_0233();
  generated_case_0234();
  generated_case_0235();
  generated_case_0236();
  generated_case_0237();
  generated_case_0238();
  generated_case_0239();
  generated_case_0240();
  generated_case_0241();
  generated_case_0242();
  generated_case_0243();
  generated_case_0244();
  generated_case_0245();
  generated_case_0246();
  generated_case_0247();
  generated_case_0248();
  generated_case_0249();
  generated_case_0250();
  generated_case_0251();
  generated_case_0252();
  generated_case_0253();
  generated_case_0254();
  generated_case_0255();
  generated_case_0256();
  generated_case_0257();
  generated_case_0258();
  generated_case_0259();
  generated_case_0260();
  generated_case_0261();
  generated_case_0262();
  generated_case_0263();
  generated_case_0264();
  generated_case_0265();
  generated_case_0266();
  generated_case_0267();
  generated_case_0268();
  generated_case_0269();
  generated_case_0270();
  generated_case_0271();
  generated_case_0272();
  generated_case_0273();
  generated_case_0274();
  generated_case_0275();
  generated_case_0276();
  generated_case_0277();
  generated_case_0278();
  generated_case_0279();
  generated_case_0280();
  generated_case_0281();
  generated_case_0282();
  generated_case_0283();
  generated_case_0284();
  generated_case_0285();
  generated_case_0286();
  generated_case_0287();
  generated_case_0288();
  generated_case_0289();
  generated_case_0290();
  generated_case_0291();
  generated_case_0292();
  generated_case_0293();
  generated_case_0294();
  generated_case_0295();
  generated_case_0296();
  generated_case_0297();
  generated_case_0298();
  generated_case_0299();
  generated_case_0300();
  generated_case_0301();
  generated_case_0302();
  generated_case_0303();
  generated_case_0304();
  generated_case_0305();
  generated_case_0306();
  generated_case_0307();
  generated_case_0308();
  generated_case_0309();
  generated_case_0310();
  generated_case_0311();
  generated_case_0312();
  generated_case_0313();
  generated_case_0314();
  generated_case_0315();
  generated_case_0316();
  generated_case_0317();
  generated_case_0318();
  generated_case_0319();
  generated_case_0320();
  generated_case_0321();
  generated_case_0322();
  generated_case_0323();
  generated_case_0324();
  generated_case_0325();
  generated_case_0326();
  generated_case_0327();
  generated_case_0328();
  generated_case_0329();
  generated_case_0330();
  generated_case_0331();
  generated_case_0332();
  generated_case_0333();
  generated_case_0334();
  generated_case_0335();
  generated_case_0336();
  generated_case_0337();
  generated_case_0338();
  generated_case_0339();
  generated_case_0340();
  generated_case_0341();
  generated_case_0342();
  generated_case_0343();
  generated_case_0344();
  generated_case_0345();
  generated_case_0346();
  generated_case_0347();
  generated_case_0348();
  generated_case_0349();
  generated_case_0350();
  generated_case_0351();
  generated_case_0352();
  generated_case_0353();
  generated_case_0354();
  generated_case_0355();
  generated_case_0356();
  generated_case_0357();
  generated_case_0358();
  generated_case_0359();
  generated_case_0360();
  generated_case_0361();
  generated_case_0362();
  generated_case_0363();
  generated_case_0364();
  generated_case_0365();
  generated_case_0366();
  generated_case_0367();
  generated_case_0368();
  generated_case_0369();
  generated_case_0370();
  generated_case_0371();
  generated_case_0372();
  generated_case_0373();
  generated_case_0374();
  generated_case_0375();
  generated_case_0376();
  generated_case_0377();
  generated_case_0378();
  generated_case_0379();
  generated_case_0380();
  generated_case_0381();
  generated_case_0382();
  generated_case_0383();
  generated_case_0384();
  generated_case_0385();
  generated_case_0386();
  generated_case_0387();
  generated_case_0388();
  generated_case_0389();
  generated_case_0390();
  generated_case_0391();
  generated_case_0392();
  generated_case_0393();
  generated_case_0394();
  generated_case_0395();
  generated_case_0396();
  generated_case_0397();
  generated_case_0398();
  generated_case_0399();
  generated_case_0400();
  generated_case_0401();
  generated_case_0402();
  generated_case_0403();
  generated_case_0404();
  generated_case_0405();
  generated_case_0406();
  generated_case_0407();
  generated_case_0408();
  generated_case_0409();
  generated_case_0410();
  generated_case_0411();
  generated_case_0412();
  generated_case_0413();
  generated_case_0414();
  generated_case_0415();
  generated_case_0416();
  generated_case_0417();
  generated_case_0418();
  generated_case_0419();
  generated_case_0420();
  generated_case_0421();
  generated_case_0422();
  generated_case_0423();
  generated_case_0424();
  generated_case_0425();
  generated_case_0426();
  generated_case_0427();
  generated_case_0428();
  generated_case_0429();
  generated_case_0430();
  generated_case_0431();
  generated_case_0432();
  generated_case_0433();
  generated_case_0434();
  generated_case_0435();
  generated_case_0436();
  generated_case_0437();
  generated_case_0438();
  generated_case_0439();
  generated_case_0440();
  generated_case_0441();
  generated_case_0442();
  generated_case_0443();
  generated_case_0444();
  generated_case_0445();
  generated_case_0446();
  generated_case_0447();
  generated_case_0448();
  generated_case_0449();
  generated_case_0450();
  generated_case_0451();
  generated_case_0452();
  generated_case_0453();
  generated_case_0454();
  generated_case_0455();
  generated_case_0456();
  generated_case_0457();
  generated_case_0458();
  generated_case_0459();
  generated_case_0460();
  generated_case_0461();
  generated_case_0462();
  generated_case_0463();
  generated_case_0464();
  generated_case_0465();
  generated_case_0466();
  generated_case_0467();
  generated_case_0468();
  generated_case_0469();
  generated_case_0470();
  generated_case_0471();
  generated_case_0472();
  generated_case_0473();
  generated_case_0474();
  generated_case_0475();
  generated_case_0476();
  generated_case_0477();
  generated_case_0478();
  generated_case_0479();
  generated_case_0480();
  generated_case_0481();
  generated_case_0482();
  generated_case_0483();
  generated_case_0484();
  generated_case_0485();
  generated_case_0486();
  generated_case_0487();
  generated_case_0488();
  generated_case_0489();
  generated_case_0490();
  generated_case_0491();
  generated_case_0492();
  generated_case_0493();
  generated_case_0494();
  generated_case_0495();
  generated_case_0496();
  generated_case_0497();
  generated_case_0498();
  generated_case_0499();
  generated_case_0500();
  generated_case_0501();
  generated_case_0502();
  generated_case_0503();
  generated_case_0504();
  generated_case_0505();
  generated_case_0506();
  generated_case_0507();
  generated_case_0508();
  generated_case_0509();
  generated_case_0510();
  generated_case_0511();
  generated_case_0512();
  generated_case_0513();
  generated_case_0514();
  generated_case_0515();
  generated_case_0516();
  generated_case_0517();
  generated_case_0518();
  generated_case_0519();
  if (!failures().empty()) {
    for (const Failure& failure : failures()) {
      std::cerr << failure.name << ": " << failure.message << "\n";
    }
    return 1;
  }
  return 0;
}

}  // namespace knotwork_generated
